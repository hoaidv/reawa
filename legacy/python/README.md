# Legacy Python App

This directory preserves the original Python implementation for reference during
the Swift migration.

- `reawa/` contains the archived package sources.
- `packaging/` contains the old `py2app` build flow.
- `requirements.txt` and `dev.sh` remain here for historical context only.

The native Swift app in the repository root is now the primary implementation.
