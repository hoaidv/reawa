---
from: dev
to: sm
date: 2026-08-27
iter: iter-005
cc: [qa]
---

# Hand-off: Developer → Scrum Master — STORY-EP-059 in-review

## Context

[STORY-EP-059](../stories/STORY-EP-059.md) (Device inverse undo ring and lastOpId) is **in-review**. One story only. Write set was `epaper/` device-document + host tests. Did not edit `infini/`, chip chrome, MASTER, execution board, or tracks.

Replaced the shipped [STORY-EP-015](../../iter-003/stories/STORY-EP-015.md) whole-tree snapshot ring. `DeviceDocument` now stores `UndoEntry { forwardOpId, seq, inverses, targets }` (depth 20), stamps `lastOpId` on document-semantic mutations, and undoes/redoes by applying stored absolute pre-op fields. Undo/redo enqueue counterpart ops (or local `compound` for multi-inverse). **0 `restore_snapshot` on undo/redo.**

Mapped all 12 scenarios in [undo-ring.feature](../../../.docs/modules/epaper/features/device-document/bdd/undo-ring.feature). Existing enclose / membership / surround / connector host tests still pass (ungroup restores captured children; connector capture restores body ink).

## Files

- `epaper/document/device_document.hpp` — inverse ring, `lastOpId`, clipboard slot, last-live-pose writer
- `epaper/tests/device_document_test.cpp` — EP-059 AC / 12-scenario host map
- `epaper/tests/enclose_test.cpp` — `newestEntry()->forwardOpId` (no snapshot `kind`)

## Asks

1. **`/qa`** to verify against [undo-ring.feature](../../../.docs/modules/epaper/features/device-document/bdd/undo-ring.feature).

## Constraints

- Ungroup inverse reparents children; it does not delete them. Copy / pan / tool / selection / clipboard-slot = 0 entries. Derived warp / last-live-pose do not bump connector `lastOpId`. Accepted `doc_load` empties undo and redo.
- Mid-gesture undo stays latched until commit.
- Local publish queue may carry `compound` for multi-inverse undo. Infini applier / wire is [STORY-EP-061](../stories/STORY-EP-061.md) ∥ [STORY-IN-038](../stories/STORY-IN-038.md) — not this story.
- `adlc audit` does **not** scan `.cpp`/`.hpp` (`CODE_EXTS`); SRS-EP-07/09 still list as orphan SRS despite `@implements` with labels. No new orphan-code rows from this story. Pre-existing: `infini/.../toolIntent.ts` → retired SRS-IN-13.
- Did **not** run `adlc rollup` (parent). Did **not** commit.

## Out of scope

EP-060 F20/F21 skip catalogue. EP-061 publish / compound wire. IN-038 Infini applier. W3 erase. TRACK-006. Chip tiles. `git push`.

## Tests

- **Ran:** `epaper/tests/run_device_document_test.sh` — all host tests pass, including `device_document_test` (EP-059, 12 scenarios), `enclose_test`, `membership_test`, `surround_create_test`, `connector_test`, `connector_warp_test`.
- **Could not run:** device/Qt `epaper_bin` build (`cmake`/`qmake` not on PATH). No RM2 panel.
