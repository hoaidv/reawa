---
from: dev
to: sm
date: 2026-08-20
iter: iter-005
cc: [qa]
---

# Hand-off: Developer → Scrum Master — STORY-IN-037 in-review

## Context

[STORY-IN-037](../stories/STORY-IN-037.md) (Infini follow Epaper — toggle, exclusion, disconnect) is **in-review**. One story only. Write set `infini/` only. Design [STORY-IN-036](../stories/STORY-IN-036.md) was already `done`. Did **not** absorb [STORY-IN-033](../stories/STORY-IN-033.md) apply-while-tablet-pans (inbound pan apply stays settle-only). No last-writer [ADR-0023](../../../.docs/adr/ADR-0023-viewport-last-writer.md).

Follow enum + toggle live in `infini/src/session/viewportFollow.ts` and `TabletSession`. Desktop FollowToggle is WindowFrame chrome (`infini/src/canvas/FollowToggle.tsx`) — not a ToolChip, not a WorldLayer child. UI-IN-04 copy/states. Viewport down is gated to `infini_to_epaper`; Infini apply-on-toggle uses the cached tablet pose after settle.

Mapped [viewport-follow-infini.feature](../../../.docs/modules/infini/features/tablet-sync/bdd/viewport-follow-infini.feature) (all 6 scenarios) in `infini/tests/viewport-follow-infini.test.ts`.

## Asks

1. **`/qa`** to verify against the feature file.
2. Do not start IN-033 until this story is `done`.

## Constraints

- Enabling Infini follow sets `epaper_to_infini` and emits `viewport_follow` on TCP 9877. Peer Epaper-follow-on → tap takeover; dual-on count 0.
- Disconnect forces `none`; hello/reconnect does not restore. Follower local-nav sets `none` then pans.
- **0** `doc_*` from this feature.
- `adlc audit`: no new orphan-code rows. Pre-existing: `infini/src/document/toolIntent.ts` → retired SRS-IN-13. SRS-IN-26/27/28 are traced. Did **not** run `adlc rollup` (parent). Did **not** commit.

## Out of scope

Apply-while-tablet-pans (IN-033). Epaper follow chrome (EP-055). `epaper/`. `macOS/`. Last-writer token.

## Tests

- **Ran:** `cd infini && npm test` (vitest) — 20 files, **122 passed**, including 7 in `viewport-follow-infini.test.ts` (6 scenarios + FollowToggle chrome).
- **Could not run:** live Electron↔RM2 TCP `:9877`; p95 ≤300 ms on a real panel (unit apply elapsed is well under the bar).
