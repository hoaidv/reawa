---
from: dev
to: sm
date: 2026-08-20
iter: iter-005
cc: [qa]
---

# Hand-off: Developer → Scrum Master — STORY-IN-033 in-review

## Context

[STORY-IN-033](../stories/STORY-IN-033.md) (Infini applies tablet viewport only while following) is **in-review**. One story only. Write set `infini/` only. Reused [STORY-IN-037](../stories/STORY-IN-037.md) follow enum + `noteFollowerLocalNav()`. Did **not** implement last-writer [ADR-0023](../../../.docs/adr/ADR-0023-viewport-last-writer.md). Did **not** start W3 (erase / Device Settings). Campaign pause for human Infini + Epaper deploy is unchanged.

Inbound apply lives in `infini/src/session/TabletSession.ts` + `viewportFollow.ts`. While `epaper_to_infini`, Infini applies tablet `translate` / uniform `scale` / `drawingRegion` immediately to WorldLayer (settle is match quality, not a gate). While `none` or `infini_to_epaper`, inbound is logged and applied **0** times (no implicit follow-on). Follower local pan/pinch on CanvasStage sets `none` **before** the gesture; **0** viewport down so Epaper’s camera is unchanged. **0** `doc_*` from apply.

Mapped [viewport-follow-apply.feature](../../../.docs/modules/infini/features/infinity-canvas/bdd/viewport-follow-apply.feature) (all 6 scenarios) in `infini/tests/viewport-follow-apply.test.ts`. Did not duplicate [viewport-follow-infini.test.ts](../../../infini/tests/viewport-follow-infini.test.ts).

## Asks

1. **`/qa`** to verify against the feature file.
2. After verify: **PAUSE** for human deploy to Infini + Epaper. Do not start W3.

## Constraints

- Apply only while `epaper_to_infini`. **0** apply while `none` (including late inbound) or `infini_to_epaper` (leader ignore logged).
- Local-nav: `none` before pan/pinch; **0** competing viewport down while follower; **0** viewport down from local-nav.
- Uniform scale (`scale_x` = `scale_y`); **0** rotation / skew.
- **0** `doc_load` / `doc_change` / `doc_snapshot` from viewport apply.
- Did **not** run `adlc rollup` (parent). Did **not** commit.

## Out of scope

Epaper follow apply (EP-039). FollowToggle chrome (IN-037, already done). `epaper/`. `macOS/`. Last-writer token. Erase / Device Settings.

## Files changed

- `infini/src/session/viewportFollow.ts` — WorldLayer pose + drawingRegion xywh helpers
- `infini/src/session/TabletSession.ts` — immediate apply while following; drawingRegion; inbound apply count
- `infini/src/session/index.ts` — exports
- `infini/src/canvas/CanvasStage.tsx` — WorldLayer apply clone; local-nav-before-gesture comments (behavior already from IN-037)
- `infini/tests/viewport-follow-apply.test.ts` — 6 scenarios 1:1
- `.plan/iter-005/stories/STORY-IN-033.md` — `in-review` (not `done`)

## Tests

- **Ran:** `cd infini && npm test` (vitest) — 21 files, **128 passed**, including **6** in `viewport-follow-apply.test.ts` (1:1 with the feature) and 7 in `viewport-follow-infini.test.ts` (no regressions).
- **Could not run:** live Electron↔RM2 TCP `:9877` (no RM2 on the agent host). Settle match is unit-level, not a live panel.

## Residual

No live TCP `:9877` / no RM2 in the agent host — QA device verify still needs a human deploy after this campaign pause.
