---
from: dev
to: sm
date: 2026-08-20
iter: iter-005
cc: [qa]
---

# Hand-off: Developer → Scrum Master — STORY-EP-055 in-review

## Context

[STORY-EP-055](../stories/STORY-EP-055.md) (Epaper follow Infini — toggle, exclusion, disconnect) is **in-review**. One story only. Write set was `epaper/`. Did not edit `infini/`, `macOS/`, design HTML, PRD, SRS, MASTER, or the execution board. No last-writer [ADR-0023](../../../.docs/adr/ADR-0023-viewport-last-writer.md).

Follow session enum + FollowToggle live in `epaper/document/viewport_follow.hpp` (host-testable). Qt chrome: `Main.qml` FollowToggle is a **sibling** of ToolChip (trailing 10 mm tile, invert when following, hatch when unavailable) — not a fourth exclusive tool. `TabletCanvasItem` taps, emits `viewport_follow` `{ direction, seq }`, adopts inbound latest-seq, forces `none` on disconnect, and does **not** restore on reconnect. EP-038/039 local-nav (`onLocalNav` then pan) is reused; local-nav now also emits `viewport_follow` while linked.

Mapped all 5 scenarios in [viewport-follow-epaper.feature](../../../.docs/modules/epaper/features/region-sync/bdd/viewport-follow-epaper.feature) in `epaper/tests/viewport_follow_test.cpp`. EP-038/039 `hand_touch_test` still passes.

## Asks

1. **`/qa`** to verify against the feature file.

## Constraints

- Default both off. Enabling Epaper follow sets `infini_to_epaper`, emits `viewport_follow`, applies cached Infini viewport (host map-apply ≤100 ms). Infini follow stays off. Exclusive tool unchanged. 0 `doc_*` from this feature.
- Takeover from `epaper_to_infini`: enum cannot be dual-on; host exclusivity ≤300 ms.
- Disconnect → `none`, toggle unavailable (hatch). Reconnect stays off until tap. Hello does not carry follow.
- Follower local-nav remains EP-038/039 (`hand_touch.hpp`). This story owns toggle + session enum + disconnect.
- `adlc audit` does **not** scan `.cpp`/`.hpp` (`CODE_EXTS`); SRS-EP-49/50/51 still list as orphan SRS despite `@implements` with labels. No new orphan-code rows from this story. Pre-existing: `infini/.../toolIntent.ts` → retired SRS-IN-13.
- Did **not** run `adlc rollup` (parent). Did **not** commit.

## Out of scope

Infini follow toggle (IN-037). Canvas match (IN-033). Last-writer ADR-0023. Device Settings / barrel. `macOS/`. REQ-15 tables. REQ-08. CHL-0011. CHL-0012. EP-032. AI.

## Tests

- **Ran:** `epaper/tests/run_device_document_test.sh` — all host tests pass, including `viewport_follow_test ok` (EP-055, 5 scenarios) and `hand_touch_test ok` (EP-038 + EP-039).
- **Could not run:** device/Qt `epaper_bin` build (`cmake`/`qmake` not on PATH). No RM2 panel. p95 ≤100 ms map-apply and p95 ≤300 ms exclusivity are host clocks, not re-measured on device.
