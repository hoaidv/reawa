---
from: dev
to: sm
date: 2026-08-20
iter: iter-005
cc: [qa]
---

# Hand-off: Developer → Scrum Master — STORY-EP-038 in-review

## Context

[STORY-EP-038](../stories/STORY-EP-038.md) (One-finger hit box: select freeform and move) is **in-review**. One story only. No EP-039 two-finger, no IN-033 Infini apply, no last-writer [ADR-0023](../../../.docs/adr/ADR-0023-viewport-last-writer.md).

Classifier + follow/pan gates live in `epaper/document/hand_touch.hpp` (host-testable). Qt wiring: `TabletAppFilter` routes one-finger touch; `TabletCanvasItem` reuses existing live-direct move/resize for box/knob, palms ≤89 du, pans locally past that.

Mapped [hand-touch-one-finger.feature](../../../.docs/modules/epaper/features/ink-box/bdd/hand-touch-one-finger.feature) in `epaper/tests/hand_touch_test.cpp` (all 7 scenarios).

## Asks

1. **`/qa`** to verify against the feature file.
2. Do not start EP-039 until this story is `done`.

## Constraints

- Publish `viewport` up only if `follow.direction = epaper_to_infini`. Default `none` → 0 up.
- Inbound Infini `viewport` applies only while `infini_to_epaper`. Follower local-nav sets `none` **then** pans.
- `adlc audit` does **not** scan `.cpp`/`.hpp` (`CODE_EXTS`); SRS-EP-21/23/25 still list as orphan SRS despite `@implements` with labels. No new orphan-code rows from this story. Pre-existing: `infini/.../toolIntent.ts` → retired SRS-IN-13.
- Did **not** run `adlc rollup` (parent). Did **not** commit.

## Out of scope

Two-finger pan/pinch (EP-039). Infini apply (IN-033). Follow-toggle chrome (EP-049/EP-053). Device Settings / barrel. `macOS/`.

## Tests

- **Ran:** `epaper/tests/run_device_document_test.sh` — all host tests pass, including `hand_touch_test ok` and `manipulate_test ok`.
- **Could not run:** device/Qt `epaper_bin` build (`cmake`/`qmake` not on PATH). No RM2 panel. p95 ≤300 ms chip and ≥5 Hz partial are the existing live-direct path, not re-measured on device.
