---
from: dev
to: sm
date: 2026-08-20
iter: iter-005
cc: [qa]
---

# Hand-off: Developer → Scrum Master — STORY-EP-039 in-review

## Context

[STORY-EP-039](../stories/STORY-EP-039.md) (Two-finger pan and pinch; publish viewport) is **in-review**. One story only. No Infini canvas match ([STORY-IN-033](../stories/STORY-IN-033.md)), no last-writer [ADR-0023](../../../.docs/adr/ADR-0023-viewport-last-writer.md).

Two-finger classifier lives in `epaper/document/hand_touch.hpp` (host-testable). Qt wiring: `TabletAppFilter` promotes a second finger (or a two-finger begin) unless box-move/resize/chip is in flight; `TabletCanvasItem` applies uniform pan/pinch to `drawingRegion` and publishes `viewport` up only while `follow.direction = epaper_to_infini`.

Mapped all 8 scenarios in [hand-touch-two-finger.feature](../../../.docs/modules/epaper/features/region-sync/bdd/hand-touch-two-finger.feature) in `epaper/tests/hand_touch_test.cpp`. EP-038 one-finger tests still pass.

## Asks

1. **`/qa`** to verify against the feature file.

## Constraints

- Publish `viewport` up only if `follow.direction = epaper_to_infini`. Default `none` → 0 up. `source: epaper`. `settle: true` on two-finger end.
- Inbound Infini `viewport` applies only while `infini_to_epaper`. Follower local-nav sets `none` **then** pans. 0 viewport down from the tablet gesture.
- Pinch: uniform scale (`scale_x == scale_y`); 0 rotation/skew (AABB stays axis-aligned).
- Second finger during box-move or resize: 0 two-finger pan until that gesture ends.
- Link down: local map still updates; 0 viewport up; `publishQueued` flag only — did not invent ToolChip/USB chrome.
- Map-apply bar is host-tested on `applyTwoFingerPanPinch` (≤100 ms), not a device panel clock.
- `adlc audit` does **not** scan `.cpp`/`.hpp` (`CODE_EXTS`); SRS-EP-24/26 still list as orphan SRS despite `@implements` with labels. No new orphan-code rows from this story. Pre-existing: `infini/.../toolIntent.ts` → retired SRS-IN-13.
- Did **not** run `adlc rollup` (parent). Did **not** commit.

## Out of scope

Infini apply (IN-033). Follow-toggle chrome (EP-049/EP-053). Last-writer ADR-0023. Device Settings / barrel. `macOS/`.

## Tests

- **Ran:** `epaper/tests/run_device_document_test.sh` — all host tests pass, including `hand_touch_test ok` (EP-038 + EP-039) and `manipulate_test ok`.
- **Could not run:** device/Qt `epaper_bin` build (`cmake`/`qmake` not on PATH). No RM2 panel. p95 ≤100 ms is the host map-apply, not re-measured on device.
