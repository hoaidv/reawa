---
from: qa
to: sm
date: 2026-08-27
iter: iter-005
cc: [dev]
---

# Hand-off: Quality Assurance Engineer → Scrum Master — STORY-EP-059 BDD authored

## Context

Gherkin is written **before** Developer. Story stays **ready**. No application code, no verify-story, no story `status` change, no MASTER / board / track edits.

[ADR-0032](../../../.docs/adr/ADR-0032-inverse-op-undo.md) (Inverse-op undo per session) is accepted. Exactness is `lastOpId` match → stored pre-op fields (geometry ±1 world unit), not a whole-tree snapshot.

## Write set

| Story | Feature | Scenarios | ACs |
|---|---|---|---|
| [STORY-EP-059](../stories/STORY-EP-059.md) Device inverse undo ring and lastOpId | [undo-ring.feature](../../../.docs/modules/epaper/features/device-document/bdd/undo-ring.feature) | 12 | 7 |

Tags: `@SRS-EP-07` (ring apply, depth, mid-gesture, load, copy, ungroup, redo); `@SRS-EP-09` (entry shape `{ forwardOpId, seq, inverses, targets }`, connector `lastOpId` not bumped by derived warp / last-live-pose).

AC map (each AC ≥1 scenario):

| AC | Scenario |
|---|---|
| 1 entry `{ forwardOpId, seq, inverses, targets }`, 0 snapshots | Structural gesture commit pushes one inverse entry; One completed gesture is one undo entry |
| 2 `lastOpId` match → pre-op fields ±1 wu, 0 divergent | Undo with matching lastOpId restores stored pre-op fields (also 0 `restore_snapshot`) |
| 3 depth 20, 21st drops oldest | Ring overflow drops the oldest entry |
| 4 pan / tool / selection / clipboard-slot; copy = 0 | Viewport tool selection clipboard-slot and copy do not push undo |
| 5 mid-gesture undo deferred | Undo requested mid-gesture is deferred |
| 6 accepted `doc_load` empties both stacks | An accepted document load empties undo and redo |
| 7 bound-node drag does not bump connector `lastOpId` | Bound-node drag does not bump connector lastOpId; Last-live-pose cache does not bump connector lastOpId |

Extra vs 1:1 AC (goal / existing file, not EP-060 F20/F21):

- Ungroup inverse does not delete children (reparent I1/I2 to stored indexes; G removed; 0 child deletions)
- Redo restores the undone forward fields (0 `restore_snapshot`; later structural commit clears redo)
- Empty redo is a no-op (existing ring contract; full empty-ring catalogue stays EP-060)

F20/F21 skip-whole / absence-partial: **not authored**. Header comment points at [STORY-EP-060](../stories/STORY-EP-060.md). Existing file had no skip scenarios to retag.

## Leftover snapshot wording (not changed)

| Path | What I left |
|---|---|
| [infini undo-ring.feature](../../../.docs/modules/infini/features/vector-document/bdd/undo-ring.feature) | Deprecated snapshot scenarios unchanged. Added a comment: EP-059 does **not** revive a desktop stack; wire-path waits for EP-061 / IN-038. |
| [one-way-sync.feature](../../../.docs/modules/epaper/features/device-document/bdd/one-way-sync.feature) | Still lists `restore_snapshot` in the transmit set (last-resort grammar, not undo). Out of this write set. |

## Asks

1. `/dev` on [STORY-EP-059](../stories/STORY-EP-059.md) against `undo-ring.feature`.
2. Do **not** start W3, TRACK-006, EP-060 / EP-061 / IN-038 this wave.
3. Chip chrome stays [ADR-0018](../../../.docs/adr/ADR-0018-undo-redo-chip-actions.md) — no tile work.

## Constraints

- Do not emit `restore_snapshot` on undo or redo. Inverse / `compound` only.
- Ungroup ≠ delete children. Copy = 0 entries. Derived warp / last-live-pose do not bump connector `lastOpId`.
- Vertical, stop `verified`. Forbidden: REQ-15, REQ-08, W3, TRACK-006.

## Out of scope

Application code. F20/F21 catalogue (EP-060). Infini undo stack. Chip tiles. `epaper/` / `infini/` source.
