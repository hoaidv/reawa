---
from: qa
to: sm
date: 2026-08-27
iter: iter-005
cc: [dev, pm]
---

# Hand-off: Quality Assurance Engineer → Scrum Master — STORY-EP-060 + STORY-EP-061 verified

[STORY-EP-060](../stories/STORY-EP-060.md) (Undo fail-safe skip and no-op catalogue) and [STORY-EP-061](../stories/STORY-EP-061.md) (Device undo queue is counterpart compound not restore snapshot) are **PASS**. Same write set / same test binary. No defect. Gherkin not rewritten. Did not edit `infini/`, application code, story `status`, MASTER, execution board, or tracks. [STORY-IN-038](../stories/STORY-IN-038.md) stays **cancelled**.

## Result

| Story | Verdict | Unmapped Gherkin | Defects | Blockers |
|---|---|---|---|---|
| [STORY-EP-060](../stories/STORY-EP-060.md) Undo fail-safe skip and no-op catalogue | **PASS** | 0 of 4 | none | none |
| [STORY-EP-061](../stories/STORY-EP-061.md) Device undo queue is counterpart compound not restore snapshot | **PASS** | 0 of 5 | none | none |
| Regression: [undo-ring.feature](../../../.docs/modules/epaper/features/device-document/bdd/undo-ring.feature) (EP-059) | **PASS** | 0 of 12 | none | none |

Not a UI story (`ui_spec` empty). No Spec/HTML grey-box spot-check.

## Command

```
epaper/tests/run_device_document_test.sh
```

Exit **0**. Includes `device_document_test: checks passed` (12 EP-059 + 4 EP-060 + 5 EP-061) plus enclose / membership / surround / connector / connector_warp / remaining host binaries in that script.

Undo host analog (printed by the same binary): `undo request→restored n=20 p50=458ns p95=1000ns` (bar 500ms).

## Leftover snapshot on undo/redo

**0.** Every mapped undo/redo host test asserts `restoreSnapshotQueued() == 0` (16 checks in `epaper/tests/device_document_test.cpp`, including nested `compound` walk). Skip and pure no-op also enqueue 0 `doc_change`. `restore_snapshot` remains last-resort **non-undo** grammar only.

## Scenario mapping — EP-060 (`undo-fail-safe.feature`)

| Feature scenario | Host test | AC |
|---|---|---|
| F21 absences no-op and live targets apply | `test_f21_absences_noop_live_targets_apply` | AC1 F21 live apply + absences no-op; consume; redo if applied; 0 error UI |
| F20 any sibling lastOpId mismatch skips the whole entry | `test_f20_sibling_lastopid_mismatch_skips_whole` | AC2 F20 skip-whole; consume; 0 redo; 0 error UI |
| Empty undo ring is a no-op | `test_empty_undo_ring_is_noop` | AC3 empty undo |
| Pure no-op undo consumes the entry and does not push redo | `test_pure_noop_undo_consumes_no_redo` | AC4 skip/pure no-op → 0 `restore_snapshot`; consume; 0 redo |

Empty **redo** (same AC3 “undo or redo”) stays in undo-ring: `test_redo_empty_is_noop` — not duplicated; still green.

## Scenario mapping — EP-061 (`undo-queue.feature`)

| Feature scenario | Host test | AC |
|---|---|---|
| Matching undo records the counterpart op | `test_matching_undo_records_counterpart` | AC1 counterpart; 0 `restore_snapshot` |
| Matching redo records the counterpart op | `test_matching_redo_records_counterpart` | AC1 counterpart; 0 `restore_snapshot` |
| Multi-inverse undo records one compound not N changes | `test_multi_inverse_undo_records_one_compound` | AC1 one `compound` of 2 counterparts |
| Path A stroke-erase commit and undo use set_ink_samples | `test_path_a_erase_commit_and_undo_set_ink_samples` | AC2 `set_ink_samples`; 0 `remove_node`; 0 snapshot |
| Path A emptying erase uses set_ink_samples and remove_node | `test_path_a_emptying_erase_compound_and_undo` | AC2 `compound` of `set_ink_samples` + `remove_node`; undo not snapshot |

EP-061 AC3 (Infini is not required to apply; IN-038 cancelled): **no Gherkin by design**. Covered by lock: 0 Infini apply scenarios; this lane did not edit `infini/`.

## Scenario mapping — EP-059 regression (`undo-ring.feature`, 12)

| Feature scenario | Host test |
|---|---|
| Structural gesture commit pushes one inverse entry | `test_undo_structural_gesture_pushes_inverse_entry` |
| One completed gesture is one undo entry | `test_undo_one_gesture_one_entry` |
| Undo with matching lastOpId restores stored pre-op fields | `test_undo_matching_lastopid_restores_pre_op_fields` |
| Viewport tool selection clipboard-slot and copy do not push undo | `test_undo_viewport_tool_selection_copy_do_not_push` |
| Ring overflow drops the oldest entry | `test_undo_ring_overflow_drops_oldest` |
| Undo requested mid-gesture is deferred | `test_undo_mid_gesture_is_deferred` |
| An accepted document load empties undo and redo | `test_undo_accepted_doc_load_clears_ring` |
| Bound-node drag does not bump connector lastOpId | `test_bound_node_drag_does_not_bump_connector_lastopid` |
| Last-live-pose cache does not bump connector lastOpId | `test_last_live_pose_does_not_bump_connector_lastopid` |
| Ungroup inverse does not delete children | `test_ungroup_inverse_does_not_delete_children` |
| Redo restores the undone forward fields | `test_redo_restores_forward_fields` |
| Empty redo is a no-op | `test_redo_empty_is_noop` |

Extra host probe (not an extra scenario): `test_undo_publishes_counterpart_not_snapshot`.

## Defects

None. No `.plan/iter-005/defects/DEF-*.md`.

## Residual risks (not DEF)

- **0 error UI** is a host analog: `errorUiShown()` is a document-layer stub that always returns 0 (skip/no-op never increment an error-UI counter). No RM2 panel / Qt chrome assertion.
- F21 construction creates B then `applyOp(remove)` (no ring push) so the undo entry still names B; Gherkin Given says B is already absent. Then clauses hold (A restored ±1 wu, B absent, 0 extra nodes, consume, redo 1, 1 `doc_change`, 0 snapshot).
- F20 mismatch is injected with raw `applyOp` (stamps `lastOpId`, does not push a second undo entry). v1 has no second writer. Then clauses hold (skip whole, origins/`lastOpId` unchanged, consume, 0 redo, 0 `doc_change`).
- Path A emptying **undo** queue-shape check allows `compound` / `set_ink_samples` / `reparent` / `remove_node` as long as type is not `restore_snapshot`. Tree restore (2 samples ±1 wu) + 0 snapshot still hold. Inner counterpart catalogue is not enumerated.
- Multi-inverse undo asserts `compound` with 2 inner ops; does not name the inner types as `set_smart_transform`.
- Device/Qt `epaper_bin` was **not** exercised for these ACs (host maps cover them). No RM2 panel.
- [STORY-IN-038](../stories/STORY-IN-038.md) cancelled — tablet→desktop undo apply not verified (out).
- `adlc audit` does not scan `.cpp`/`.hpp` (`CODE_EXTS`); SRS-EP-07/08/13 still list as orphan SRS despite `@implements`. Pre-existing: `infini/.../toolIntent.ts` → retired SRS-IN-13.
- [one-way-sync.feature](../../../.docs/modules/epaper/features/device-document/bdd/one-way-sync.feature) header still lists `restore_snapshot` in the transmit set (last-resort grammar, not undo). Out of this write set.

## Asks

1. **`/sm`** — record EP-060 + EP-061 **verified**. Parent owns story `status` / board / MASTER. Do **not** start W3, TRACK-006, or Infini undo apply.
2. **`/pm`** may gate-close both stories.

## Constraints

- Did not edit `epaper/` / `infini/` source, Gherkin, PRD, SRS, MASTER, execution board, tracks, or story `status`.
- Did not run `adlc rollup`. Did not git commit.
- Vertical, stop `verified`. Forbidden: REQ-15 tables, REQ-08, CHL-0011, CHL-0012, EP-032, AI, last-writer ADR-0023, TRACK-006 reopen, DeviceMap invert UI, Mouse DragHandler, STORY-IN-038, infini undo apply, tablet-to-desktop undo sync.

## Out of scope

Application code. `infini/`. W3. TRACK-006. IN-038. Story status / execution board / MASTER.
