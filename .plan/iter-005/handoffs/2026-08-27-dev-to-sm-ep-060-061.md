---
from: dev
to: sm
date: 2026-08-27
iter: iter-005
cc: [qa]
---

# Hand-off: Developer → Scrum Master — STORY-EP-060 + STORY-EP-061

## Context

Both stories implemented in one write set (`epaper/document/device_document.hpp` + host tests). Did **not** edit `infini/`, story `status`, MASTER, execution board, or tracks. [STORY-IN-038](../stories/STORY-IN-038.md) stays cancelled.

- [STORY-EP-060](../stories/STORY-EP-060.md) (Undo fail-safe skip and no-op catalogue) — F20 skip-whole, F21 absence-partial, empty undo, pure no-op consume / 0 redo / 0 error UI / 0 `restore_snapshot`.
- [STORY-EP-061](../stories/STORY-EP-061.md) (Device undo queue is counterpart compound not restore snapshot) — matching undo/redo enqueue the counterpart; multi-inverse undo is one `compound`; Path A erase is `set_ink_samples` (`compound` + `remove_node` only when emptied).

Mapped all **4** scenarios in [undo-fail-safe.feature](../../../.docs/modules/epaper/features/device-document/bdd/undo-fail-safe.feature) and all **5** in [undo-queue.feature](../../../.docs/modules/epaper/features/device-document/bdd/undo-queue.feature). The **12** EP-059 scenarios in [undo-ring.feature](../../../.docs/modules/epaper/features/device-document/bdd/undo-ring.feature) still pass. Gherkin was not rewritten.

## Files

- `epaper/document/device_document.hpp` — F20/F21 classify; `commitGesture` / `commitParts`; `set_ink_samples` + Path A `commitPathAErase`; compound publish; `applyOp` stamps `lastOpId` (mismatch fixture without a second undo entry)
- `epaper/tests/device_document_test.cpp` — 4 + 5 host maps; existing 12 unchanged

## Asks

1. **`/qa`** to verify EP-060 + EP-061 against the two new features **and** the 12 undo-ring scenarios.
2. Parent SM owns story status / board / MASTER. Dev did **not** set `in-review` or run `adlc rollup`.

## Constraints

- F21: some targets absent, none changed → apply live, no-op absences, consume, redo only if something applied, 0 error UI.
- F20: any sibling `lastOpId` mismatch → skip whole, consume, 0 redo, 0 `doc_change`, later fields unchanged.
- Skip or pure no-op: 0 `restore_snapshot`, 0 `doc_change`, 0 redo.
- Applied undo/redo: counterpart or one `compound` of counterparts — never `restore_snapshot`.
- Path A: `set_ink_samples`; `remove_node` only if emptied. 0 snapshot fallback.
- v1: one device document-epoch stack. Infini is not a second author.

## Out of scope

`infini/`. Tablet→desktop undo apply. W3 erase UI / Device Settings. TRACK-006. IN-038. Story status / execution board / MASTER.

## Tests

- **Ran:** `epaper/tests/run_device_document_test.sh` — all host tests pass, including `device_document_test` (12 EP-059 + 4 EP-060 + 5 EP-061), `enclose_test`, `membership_test`, `surround_create_test`, `connector_test`, `connector_warp_test`.
- **Could not run:** device/Qt `epaper_bin` build (`cmake`/`qmake` not on PATH). No RM2 panel.

## Leftover snapshot on undo/redo path

**0.** `applyHistoryEntry` / `commitParts` / Path A never enqueue `restore_snapshot`. `restoreSnapshotQueued()` also walks nested `compound` ops. `restore_snapshot` remains last-resort **non-undo** grammar only.

## Surprises

- Multi-node `set_smart_transform` is one gesture via `commitGesture` (N inverses, one `compound` on the wire if N>1), not a forked op shape.
- F20 mismatch is injected with raw `applyOp` (stamps `lastOpId`, does not push the ring). v1 has no second writer; this is the revision-skip predicate, not a peer identity.
- Path A emptying undo publishes a compound of restore (`reparent` with stored body) + `set_ink_samples` — counterparts, not a tree dump.
- `adlc audit` does **not** scan `.cpp`/`.hpp` (`CODE_EXTS`); SRS-EP-07/08/13 still list as orphan SRS despite `@implements` with labels. No new orphan-code rows from this story. Pre-existing: `infini/.../toolIntent.ts` → retired SRS-IN-13.
- Did **not** run `adlc rollup` (parent). Did **not** commit.
