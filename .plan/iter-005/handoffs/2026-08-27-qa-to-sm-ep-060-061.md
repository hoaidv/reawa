---
from: qa
to: sm
date: 2026-08-27
iter: iter-005
cc: [dev]
---

# Hand-off: Quality Assurance Engineer → Scrum Master — STORY-EP-060 + STORY-EP-061 BDD authored

## Context

Gherkin is written **before** Developer. Stories stay **ready**. No application code, no verify-story, no story `status` change, no MASTER / board / track edits. `infini/` not touched.

[STORY-IN-038](../stories/STORY-IN-038.md) is **cancelled**. **0** Infini apply / unknown-desktop-op scenarios.

Existing [undo-ring.feature](../../../.docs/modules/epaper/features/device-document/bdd/undo-ring.feature) still has **12** scenarios (EP-059). Header comment now points at the new files; scenarios unchanged.

## Write set

| Story | Feature | Scenarios | ACs covered |
|---|---|---|---|
| [STORY-EP-060](../stories/STORY-EP-060.md) Undo fail-safe skip and no-op catalogue | [undo-fail-safe.feature](../../../.docs/modules/epaper/features/device-document/bdd/undo-fail-safe.feature) | **4** | 4 |
| [STORY-EP-061](../stories/STORY-EP-061.md) Device undo queue is counterpart compound not restore snapshot | [undo-queue.feature](../../../.docs/modules/epaper/features/device-document/bdd/undo-queue.feature) | **5** | 2 of 3 as Gherkin; AC3 is a non-requirement |

Tags: `@SRS-EP-13` (EP-060 skip/no-op/empty/0 error UI); `@SRS-EP-08` (EP-061 outbound queue). Feature-level also carries `@SRS-EP-07` (parent_srs).

### EP-060 AC map

| AC | Scenario |
|---|---|
| F21 live apply + absences no-op; consume; 0 error UI; redo if applied | F21 absences no-op and live targets apply |
| F20 any sibling `lastOpId` mismatch → skip whole; consume; 0 redo; 0 error UI | F20 any sibling lastOpId mismatch skips the whole entry |
| Empty ring undo or redo is a no-op | Empty undo ring is a no-op (**new**). Empty redo already in undo-ring.feature — not duplicated |
| Skip or pure no-op → 0 `restore_snapshot` | Then-clauses on F20 + Pure no-op undo consumes the entry and does not push redo |

### EP-061 AC map

| AC | Scenario |
|---|---|
| Matching undo/redo outbound is counterpart or `compound`; 0 `restore_snapshot` | Matching undo records the counterpart op; Matching redo records the counterpart op; Multi-inverse undo records one compound not N changes |
| Path A stroke-erase forward/inverse `set_ink_samples`; `remove_node` only if emptied; 0 snapshot fallback | Path A stroke-erase commit and undo use set_ink_samples; Path A emptying erase uses set_ink_samples and remove_node |
| Infini is not required to apply those ops (IN-038 cancelled) | **No scenario** — device queue only; comment in feature header |

## Leftover snapshot wording

| Path | What remains |
|---|---|
| [one-way-sync.feature](../../../.docs/modules/epaper/features/device-document/bdd/one-way-sync.feature) | Header still lists `restore_snapshot` in the transmit set (last-resort grammar, not undo). Does not yet list `compound` / `set_ink_samples`. Out of this write set. |
| [infini undo-ring.feature](../../../.docs/modules/infini/features/vector-document/bdd/undo-ring.feature) | Deprecated snapshot scenarios. **Not edited** (`infini/` lock). |
| undo-ring / undo-fail-safe / undo-queue | `restore_snapshot` appears only as **0 queued** / **must not emit** assertions. |

## Asks

1. `/dev` on [STORY-EP-060](../stories/STORY-EP-060.md) then [STORY-EP-061](../stories/STORY-EP-061.md) against the new feature files. Do not regress undo-ring.feature's 12.
2. Do **not** start W3, TRACK-006, or Infini undo apply.
3. Parent SM owns story status / board / MASTER.

## Constraints

- Vertical, stop `verified`. Forbidden: IN-038, W3, TRACK-006, REQ-15 tables, REQ-08.
- Skip and pure no-op consume the entry, push **0** redo, publish **0** `doc_change`, show **0** error UI.
- Applied undo/redo enqueue counterpart or `compound`, never `restore_snapshot`.
- Path A never falls back to snapshot.

## Out of scope

Application code. `infini/`. W3. TRACK-006. IN-038. Story status / execution board / MASTER.
