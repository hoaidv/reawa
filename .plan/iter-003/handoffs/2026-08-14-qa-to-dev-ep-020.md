---
from: qa
to: dev
date: 2026-08-14
iter: iter-003
cc: [sm]
verdict: READY
---

# Hand-off: QA → Dev — STORY-EP-020 BDD (author)

## Context

Author-mode BDD for [STORY-EP-020](../stories/STORY-EP-020.md) (`status: ready`). Pickup:
[SM → QA W12](./2026-08-14-sm-to-qa-w12.md). Parent:
[SRS-EP-08](../../../.docs/modules/epaper/features/device-document/srs-logic.md#srs-ep-08-one-way-sync),
[ADR-0015](../../../.docs/adr/ADR-0015-one-way-sync-contract.md). Envelopes:
[SRS-IN-09](../../../.docs/modules/infini/features/vector-document/srs-data.md).

Story status left **`ready`**. Do not mark `in-progress` / `done` from this walk.

Sibling [STORY-IN-028](../stories/STORY-IN-028.md) is **out of this lane** (other QA).
ADR-0019 amend for [CHL-0018](../challenges/CHL-0018-live-node-tool-canvas.md) does **not** gate W12.

## Coverage verdict

**READY-FOR-DEV.** Eight ACs → eight `@SRS-EP-08` scenarios in
[one-way-sync.feature](../../../.docs/modules/epaper/features/device-document/bdd/one-way-sync.feature).

`adlc audit` BDD row: **`[SRS-EP-08]` 8 scenarios — covered** (was 11 architect drafts; folded 1:1 to story AC).
Orphan SRS-EP-08 in code is expected until `/dev` lands `@implements`.

## AC ↔ scenario

| # | AC (story) | Scenario | Tag |
|---|---|---|---|
| 1 | After initial load: 0 inbound document-bearing; viewport still applies | After the initial load, only viewport comes down | `@SRS-EP-08` |
| 2 | Unsolicited `doc_load` / `doc_snapshot` / `pickables` / `tool_intent` → not applied, logged, local tree unchanged | Unsolicited document inbound is rejected | `@SRS-EP-08` |
| 3 | 5 queued; desktop offers load → 5 `doc_change` in seq, `queue_empty`, then accept (0 discard); seq 0; undo + selection clear; `load_ack` | Load handshake drains the queue then starts a new epoch | `@SRS-EP-08` |
| 4 | Session down; 10 ops → 10 local, 10 queued, 0 tools unavailable | Editing continues with the session down | `@SRS-EP-08` |
| 5 | Restored link; `drain_ack` → queued ops in seq (0 lost, 0 reordered); duplicate `opId` no-op | Reconnect publishes the queue; duplicate opId is a no-op | `@SRS-EP-08` |
| 6 | Committed structural op → exactly one `doc_change {seq, opId, op, baseSeq}`; mirror p95 ≤300 ms | One doc_change per committed structural op | `@SRS-EP-08` |
| 7 | In-progress stroke `stroke_*` → no `stroke_begin.intent`; node only in pen-up `doc_change`; 0 preview paths on the mirror | Preview stroke_* is not a document change | `@SRS-EP-08` |
| 8 | Gesture in flight + `doc_load` offered → commit first; change publishes before load accept | A load offered mid-gesture is deferred | `@SRS-EP-08` |

## Asks

1. `/dev` [STORY-EP-020](../stories/STORY-EP-020.md) — **epaper/** publish queue + handshake only.
2. Implement so all eight `@SRS-EP-08` scenarios can pass. Set story `in-review` when they do → `/qa` verify. **Do not** mark `done`.
3. Do **not** implement IN-028 (`infini/` `doc_load` offerer).

## Constraints

- **SRS-IN-09 transmit names** only. Handshake: `hello`, `drain_ack`, `doc_change`, `queue_empty`, `doc_load`, `load_ack`. Preview: `stroke_begin` / `stroke_point` / `stroke_end` (no `intent`). Viewport always. Op `type`: `append_ink`, `create_smart_group`, `set_smart_transform`, `set_ink_scale_mode`, `reparent`, `remove_node`, `restore_snapshot`. Envelope `{ type: "doc_change", seq, opId, op, baseSeq }`.
- Do not invent a third op-type alias (CHL-0009). Do not revive `doc_snapshot` / `pickables` / `tool_intent`.
- **0** queued discards. Drain before accept. Mid-gesture load defers until commit.
- Session down: editing continues; **0** tools gated on the link.
- ADR-0019 / CHL-0018 is not a W12 gate.

## Out of scope

- STORY-IN-028 BDD or code
- Application code in this QA walk
- Flipping EP-020 to `in-progress` / `done`
- Architect ADR-0019 amend
