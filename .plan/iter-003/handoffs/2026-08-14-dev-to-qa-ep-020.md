---
from: dev
to: qa
date: 2026-08-14
iter: iter-003
---

# Hand-off: Dev → QA — STORY-EP-020

## Context

Device one-way sync for [STORY-EP-020](../stories/STORY-EP-020.md) / [SRS-EP-08](../../../.docs/modules/epaper/features/device-document/srs-logic.md#srs-ep-08-one-way-sync). Host tests map the eight `@SRS-EP-08` scenarios in [one-way-sync.feature](../../../.docs/modules/epaper/features/device-document/bdd/one-way-sync.feature). Story `status: in-review`. IN-028 not touched. ADR-0019 / CHL-0018 not gated.

Core: `epaper/document/one_way_sync.hpp`. Wire: `StrokeSync` + `TabletCanvasItem` (no `stroke_begin.intent`). Run: `epaper/tests/run_device_document_test.sh`.

## Asks

1. `/qa` verify STORY-EP-020 — do not mark `done` from Dev.
2. Confirm inbound classification, drain-before-load, session-down queue, reconnect `drain_ack`, one `doc_change` per commit, preview `stroke_*`, mid-gesture load deferral.

## Constraints

- SRS-IN-09 transmit names only (`hello`, `drain_ack`, `doc_change`, `queue_empty`, `doc_load`, `load_ack`, `stroke_*`).
- 0 queued discards. Viewport still applies after the initial load.

## Out of scope

- STORY-IN-028
- BDD edits
- ADR-0019 amend
