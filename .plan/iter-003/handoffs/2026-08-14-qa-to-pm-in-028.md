---
from: qa
to: pm
date: 2026-08-14
iter: iter-003
cc: [sm, dev]
verdict: PASS
---

# Hand-off: QA → PM — STORY-IN-028 done (handshake-gated doc_load)

## Verdict

**PASS.** [STORY-IN-028](../stories/STORY-IN-028.md) set **`done`**. Not a UI story — no Spec/HTML
grey-box check. Sibling [STORY-EP-020](../stories/STORY-EP-020.md) was **not** verified.

Host: `cd infini && npm test` — **PASS**. 16 files, **99 passed**. Story suite
`tests/handshake-doc-load.test.ts` — **5/5** mapped to
[handshake-doc-load.feature](../../../.docs/modules/infini/features/tablet-sync/bdd/handshake-doc-load.feature)
(`@SRS-IN-07` / `@SRS-IN-08`; wire names from [SRS-IN-09](../../../.docs/modules/infini/features/vector-document/srs-data.md)).

## AC → tests

| AC | Unit test |
|---|---|
| queued > 0 → `drain_ack` first, apply `doc_change` in seq, `doc_load` only after `queue_empty` | `sends drain_ack, applies doc_change in seq, then doc_load after queue_empty` |
| After load, 0 further outbound document messages (viewport-only down) | `after handshake, only viewport may flow down` |
| Reconnect does not push a document — handshake (drain then load) | `waits for hello and drains before one doc_load` |
| Orientation / Infini-side action → 0 `doc_load` / `doc_snapshot` | `does not emit doc_load or doc_snapshot` |
| Retired `doc_snapshot`, `pickables`, `tool_intent` not emitted | `emits doc_load and never retired wire names` |

`IpcRmTransport` sends `drain_ack` / `doc_load` / viewport; `sendDocOp` remains a no-op. Load
envelope is `{ type: "doc_load", document, seq: 0 }` with no `pickables`.

## BDD

Un-tagged `@future` on `session-channels.feature` scenario **Infini authors no document ops during
a session** — epoch `doc_load` is now shipped.

## Asks

1. `/pm` gate-close for STORY-IN-028.
2. Do not mix EP-020 (other lane; still implementing).

## Out of scope

- STORY-EP-020 device handshake
- Viewport publish regression (existing tablet-session / viewport suites still green)
