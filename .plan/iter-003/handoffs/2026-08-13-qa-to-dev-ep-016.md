---
from: qa
to: dev
date: 2026-08-13
iter: iter-003
cc: [sm]
verdict: READY
---

# Hand-off: QA → Dev — STORY-EP-016 BDD walk (review only)

## Context

Author-mode coverage walk on [STORY-EP-016](../stories/STORY-EP-016.md) (`status: ready`).
Feature file already existed; **not rewritten**. Story status left **`ready`** — Dev has not started.

Pickup from [SM → QA](./2026-08-13-sm-to-qa-ep-016.md). SM → Dev pickup also landed:
[2026-08-13-sm-to-dev-ep-016.md](./2026-08-13-sm-to-dev-ep-016.md).

Home: [enclose-recognition.feature](../../../.docs/modules/epaper/features/ink-box/bdd/enclose-recognition.feature)
(`@SRS-EP-10`, eight scenarios). Parent: [SRS-EP-10](../../../.docs/modules/epaper/features/ink-box/srs-logic.md)
enclose only (not draw-into membership). Quality bars: [SRS-EP-14](../../../.docs/modules/epaper/features/ink-box/srs-quality.md)
create + consecutive + cross-implementation.

Infini [vector-document/bdd/enclose-recognition.feature](../../../.docs/modules/infini/features/vector-document/bdd/enclose-recognition.feature)
(`@SRS-IN-10` `@deprecated`) is **evidence only** — do not run in the device suite; do not treat as live device AC.

## Coverage verdict

**READY-FOR-DEV.** Every EP-016 behavioral AC has a matching `@SRS-EP-10` scenario. No Gherkin added or rewritten.

AC 5 (shared `enclose/` fixtures) has **no Gherkin** by design: the corpus is a Dev JSON deliverable. Missing `enclose/` does **not** block READY-FOR-DEV.

## AC ↔ scenario

| # | AC (story) | Scenario | Tag | Notes |
|---|---|---|---|---|
| 1 | Ink-box armed at pen-down; pen-up guards pass → `create_smart_group` commits locally with **0 desktop messages**; enclose stroke `role: boundary`; captured ink `role: content` with `layoutOffset` UV; bounds = fitted AABB; box visible **p95 ≤500 ms** after pen-up | **Successful enclose commits Smart Group immediately and locally** | `@SRS-EP-10` | Also asserts no propose/accept. p95 ≤500 ms is [SRS-EP-14](../../../.docs/modules/epaper/features/ink-box/srs-quality.md) Creation — measure in device/unit tests |
| 2 | Pen armed, **or** stroke too small / empty → **0** boxes; stroke stays ordinary ink (**0** error banner) | **Stroke drawn with Pen armed skips recognition** + **Failed guards leave ordinary ink** | `@SRS-EP-10` | Split across two scenarios (Pen skip vs size/content guards). Matches SRS-EP-10 trigger table + guard-fail no-op |
| 3 | Ink-box armed at pen-down; tool switches before pen-up → stroke still evaluated as an enclose | **Tool switched mid-stroke does not change what the stroke means** | `@SRS-EP-10` | Pen-down latch ([SRS-EP-10](../../../.docs/modules/epaper/features/ink-box/srs-logic.md) Trigger dispatch) |
| 4 | 10 consecutive successful encloses → **10 / 10** boxes; **0** desync; **0** lost boxes (CHL-0007) | **Consecutive encloses stay correct** | `@SRS-EP-10` | [SRS-EP-14](../../../.docs/modules/epaper/features/ink-box/srs-quality.md) Creation consecutive row |
| 5 | Shared fixtures `enclose/` → device and desktop guard verdict + fitted bounds agree **100%** | *(no Gherkin — Dev JSON corpus)* | — | See **Fixtures** below. Do not invent a ninth scenario |

### Extra SRS coverage (present; not story AC)

SM asked these to be confirmed. They exist; leave them.

| Scenario | Tag | Why it stays |
|---|---|---|
| **Ink already inside a Smart Group is skipped** | `@SRS-EP-10` | SRS-EP-10 already-grouped guard; remaining ungrouped ink still captures |
| **Enclose works with the session down** | `@SRS-EP-10` | “0 desktop messages” / session down = no peer required; queue `create_smart_group` locally |
| **Undo restores the pre-create tree** | `@SRS-EP-10` | SRS-EP-10 undo; one `create_smart_group` via `commitOp` so EP-015 ring gets one entry. **No on-panel undo chrome** (CHL-0010) |

Scenarios tag `@SRS-EP-10` only. SRS-EP-14 quality bars (500 ms, 0 peer messages, 10/10, fixture agreement) are asserted inside those EP-10 scenarios or as the fixture corpus — not a rewrite.

## Fixtures (Dev deliverable — not a Gherkin gap)

`enclose/` JSON is **missing**. `ops/` exists:

| Location | Status |
|---|---|
| `.docs/modules/infini/features/vector-document/fixtures/ops/` | present (`append_ink.json`, `create_primitive.json`) |
| `epaper/tests/fixtures/ops/` | present (same pair + README) |
| `infini/tests/fixtures/ops/` | present (same pair) |
| `**/fixtures/enclose/` | **absent** (docs, epaper tests, infini tests) |

Add the corpus the same way as `ops/`:

1. Canonical: `.docs/modules/infini/features/vector-document/fixtures/enclose/`
2. Copies: `epaper/tests/fixtures/enclose/` + `infini/tests/fixtures/enclose/`

Both peers run the same strokes. Assert guard verdict + fitted bounds **100%** ([SRS-EP-14](../../../.docs/modules/epaper/features/ink-box/srs-quality.md) Cross-implementation; [SRS-EP-09](../../../.docs/modules/epaper/features/device-document/srs-data.md) Shared fixtures). Disagreement is a `CHL-*`, not a widened tolerance.

## Asks

1. `/dev` [STORY-EP-016](../stories/STORY-EP-016.md) — on-device enclose in `epaper/` only.
2. Implement so all eight `@SRS-EP-10` scenarios in `enclose-recognition.feature` can pass.
3. Author shared `enclose/` fixture JSON (canonical + both test trees) and run both peers against it.
4. Set the story `in-review` when tests pass → `/qa` verify. **Do not** mark `done`.
5. After ship: leave a **non-synth** epaper binary on RM2 for human drawing. **STOP.** Do not flip EP-017.

## Constraints

- Recognition is **not** an op. Only `create_smart_group` publishes / queues. **0** peer round trips inside the gesture.
- Device trigger is **Ink-box armed at pen-down**, not wire `intent`. Port guards from `infini/src/document/recognizeEnclose.ts` (MIN 48 wu, ≥80% samples, skip already-grouped).
- Commit via **`commitOp`** so undo (EP-015) gets one ring entry.
- Failed guards → ordinary ink, **0** banner.
- Do not edit `infini/src` except adding **shared fixture JSON** if needed for parity tests.
- Do not put recognition in `paint()`; keep ingest after `flushPending()`.
- Op name: `create_smart_group` (SRS-IN-09). No propose/accept.
- **No on-panel undo chrome** (CHL-0010).
- Residue EP-007…011 / IN-020…026 is out of this story.

## Out of scope

- Rewriting `enclose-recognition.feature` or inventing Gherkin for `enclose/` fixtures
- Flipping EP-016 to `done` or `in-progress` (Dev sets `in-progress` / `in-review`)
- Application code in this QA walk
- Draw-into membership (EP-017), selection-create (EP-018 frozen), manipulation (EP-019), handshake (EP-020)
- Treating Infini `@SRS-IN-10` as live device AC

## Gaps

None that block READY-FOR-DEV. `enclose/` fixture JSON is a Dev deliverable, noted above. No CHL filed.

`adlc audit`: orphan `[SRS-EP-10]` / `[SRS-EP-14]` (and sibling EP-*.hpp) is expected until Dev lands `@implements` — scanner `CODE_EXTS` also omits `.hpp`/`.cpp`. Not a CHL.
