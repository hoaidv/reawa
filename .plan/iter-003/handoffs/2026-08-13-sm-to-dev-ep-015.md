---
from: sm
to: dev
date: 2026-08-13
iter: iter-003
cc: [qa]
---

# Hand-off: SM → Dev — STORY-EP-015 undo ring (after QA walk)

## Context

[STORY-EP-014](../stories/STORY-EP-014.md) **done**. RM2: ingest p95=231 µs, ink_nodes=40,
arrival→flush p95=1392 µs, dropped=0, paint-loop-hits=0. Latency floor still outranks undo.

## Pickup

| Lane | Story | Status | Writes |
|---|---|---|---|
| **C** | [STORY-EP-015](../stories/STORY-EP-015.md) | **ready** | `epaper/` snapshot undo ring |

Wait for the QA walk on `undo-ring.feature` if it has not run yet. Then implement.

## Lane C — EP-015

- Logic: `device-document/srs-logic.md` `[SRS-EP-07]` gesture-commit path + undo
- BDD: `device-document/bdd/undo-ring.feature` `@SRS-EP-07`
- Whole-document snapshots, depth **20**, overflow drops oldest
- One completed gesture = one ring entry (intermediate frames do not push)
- Viewport / tool / selection must not push
- Mid-gesture undo is latched, then runs after commit
- Undo replaces the tree with the popped snapshot and **queues** `restore_snapshot`
- Accepted `doc_load` empties the ring (hook; handshake is EP-020)
- **No on-panel chrome** ([CHL-0010](../challenges/CHL-0010-undo-vs-selection-create-chrome.md))

## Constraints

- Do not edit `infini/`
- Do not ship EP-020 TCP/handshake / `doc_load` accept UI
- Do not add a fourth ToolChip slot or undo button
- Do not start enclose (EP-016)
- Ingest still after `flushPending()`; do not put undo work in `paint()`
- No peer round trip inside a gesture
- Op names stay SRS-IN-09 transmit set (`restore_snapshot` — no third alias)

## After code

Set the story `in-review` and hand QA. Do not flip EP-016 to `ready` (SM does that).
