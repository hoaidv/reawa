---
from: dev
to: qa
date: 2026-08-14
iter: iter-003
---

# Hand-off: Dev → QA — EP-019

## What shipped

On-device live manipulation via a **capability descriptor** (`document/capability.hpp`) and
kind-agnostic router (`document/manipulate.hpp`). Press on a SmartGroup with `sel_rect` or
`sel_freeform` selects/moves/resizes; empty canvas still marquee/lasso (EP-018).

Live ink follows the pen (partial region redraw, not `rasterizeVectors` full clear). Release
restores origin then one `set_smart_transform` (rotation reserved 0). Handles 28/56 du; LOD 96 du
→ `manipulationUnavailable`. `fixedInk` / `withBounds` mapping matches Infini `selection.ts`.

## Host tests

`epaper/tests/run_device_document_test.sh` includes `manipulate_test` — **PASS**.

## Asks

Verify `@SRS-EP-11` on host; on-panel: select both tools, move ≥5 Hz, both resize modes, invert
handle, deselect 0 residue, LOD banner. Do not regress EP-018 Enclose.

## Next

`/qa` verify.
