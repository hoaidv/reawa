---
from: sm
to: qa
date: 2026-08-14
iter: iter-003
---

# Hand-off: SM → QA

## Context

W11a implement for [STORY-EP-018](../stories/STORY-EP-018.md) is **in-review**. Host suite PASS
(`surround_create_test` + device-document). Human has been drawing on RM2; Dev patched three
on-panel bugs and the ≥80% membership lock. **Do not open W11b (EP-019)** until this verify PASS.

BDD: [selection-create-surround.feature](../../../.docs/modules/epaper/features/ink-box/bdd/selection-create-surround.feature)
(rect ≥80% inside, freeform ≥80% inside polyline).

## Asks

On-panel with the **latest** ARM binary (`epaper/build/bin/epaper` after last `./scripts/build.sh`):

1. **Rect / freeform membership** — only ink with **≥80% of samples** inside the rubber-band or
   closed polyline is selected. A graze / AABB touch must **not** select. HUD: `sel=N …` or
   `sel=0 (no nodes ≥80% inside)`.
2. **Settled chrome** — after a real lasso (including a loop that ends near the start), if
   `sel≥1`, `ovl.nodes_bounds` + 6 anchors + Enclose must appear. Tap (`sel=0 (tap)`) clears.
3. **Enclose** — tap CTA: box created; **no leftover dashed rect** in the middle; HUD shows
   `[enclose] … outcome=created id=sg_sel_…`. Refuse: SmartGroup in selection or no surround;
   selection stays; reason visible.
4. **ToolChip** — exactly four tools; Enclose is **not** a fifth chip.
5. **swtcon “generator fallen behind”** — note if chrome stays stale for seconds (fail) vs
   occasional stderr only (pass / not a story fail).

## Constraints

- Redeploy the rebuilt binary; QML/icons are in that image.
- USB: tablet `10.11.99.1`, Mac `10.11.99.12` (`en7`). Do not point Device Log at Infini `:9877`.

## Out of scope

- Nested enclose (CHL-0011); live 6-anchor resize (EP-019); undo chrome (CHL-0010).

## Next

PASS → SM flips EP-018 `done`, then W11b EP-019 (or stop for human). FAIL → stay in-review, `/dev`.
