---
from: qa
to: pm
date: 2026-08-14
iter: iter-003
cc: [sm, dev]
verdict: PASS
---

# Hand-off: QA → PM — STORY-EP-020 done (device one-way sync)

## Verdict

**PASS.** [STORY-EP-020](../stories/STORY-EP-020.md) set **`done`**. Not a UI story — no Spec/HTML
grey-box check. Sibling [STORY-IN-028](../stories/STORY-IN-028.md) was **not** re-verified (already QA-done).

Host: `epaper/tests/run_device_document_test.sh` — **PASS**. `one_way_sync_test` **OK** (8/8 mapped
to [one-way-sync.feature](../../../.docs/modules/epaper/features/device-document/bdd/one-way-sync.feature)
`@SRS-EP-08`). Prior device-document host tests still green. `doc_change` commit→emit p95 **0 µs**
on host (bar 300 ms).

## AC → tests

| AC | Host test |
|---|---|
| After initial load: 0 inbound document-bearing; viewport still applies | `test_after_initial_load_only_viewport` |
| Unsolicited `doc_load` / `doc_snapshot` / `pickables` / `tool_intent` rejected, logged, tree unchanged | `test_unsolicited_document_inbound_rejected` |
| 5 queued; drain then `queue_empty`; load accept; 0 discard; seq 0; undo + selection clear; `load_ack` | `test_load_handshake_drains_then_new_epoch` |
| Session down; 10 ops local + queued; 0 tools unavailable | `test_editing_continues_session_down` |
| Restored link; `drain_ack` publishes in seq; duplicate `opId` no-op | `test_reconnect_publishes_queue_duplicate_noop` |
| One `doc_change {seq, opId, op, baseSeq}`; mirror p95 ≤300 ms | `test_one_doc_change_per_structural_op` |
| Preview `stroke_*` no `intent`; node only in pen-up `doc_change`; 0 preview on mirror | `test_preview_stroke_not_document_change` |
| Mid-gesture `doc_load` deferred until commit publishes first | `test_load_mid_gesture_deferred` |

## Audit

`adlc audit` + `rollup` after sign-off. `[SRS-EP-08]` still listed as orphan SRS in the scanner
(C++ `@implements` in headers is present in `one_way_sync.hpp` / `device_document.hpp` / canvas
wire). No defect — scanner gap, not a missing implementation. No `DEF-*` filed.

## Asks

1. `/pm` gate-close for STORY-EP-020.
2. IN-028 is already `done`; do not mix this close with a second IN-028 verify.

## Out of scope

- STORY-IN-028
- Source edits
- ADR-0019 / CHL-0018
- Device-on-panel latency (host analog only)
