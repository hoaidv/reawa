---
from: qa
to: dev
date: 2026-08-13
iter: iter-003
cc: [sm]
verdict: READY
---

# Hand-off: QA → Dev — W9 BDD walk (EP-014 ∥ IN-027)

## Context

W9 ungated (EP-013 done). Review-only of architect BDD; no rewrite of
`undo-ring.feature` / `one-way-sync.feature` / `session-channels.feature`. Infini
`@deprecated` originals left in place. Stories stay **ready** — not flipped.

New file: [ingest-stroke.feature](../../../.docs/modules/epaper/features/device-document/bdd/ingest-stroke.feature).

## Coverage

### STORY-EP-014 — device tree + stroke ingestion

Home: **new** `epaper/features/device-document/bdd/ingest-stroke.feature`.

| AC | Scenario | Tag | Home |
|---|---|---|---|
| 20 strokes, no session → Ink nodes p95 ≤50 ms after pen-up; panel paints that tree; 0 inbound peer pictures | Finished strokes become Ink nodes with no session | `@SRS-EP-07` | **new** |
| Selection armed, pen moves → no stroke begins, no Ink ingested | Selection armed does not ingest ink | `@SRS-EP-07` | **new** |
| Digitizer channels (pressure, tilt, extras) survive 100%; preview may omit | Digitizer channels survive on the stored node | `@SRS-EP-09` | **new** |
| Shared `ops/` fixtures: device vs desktop trees agree 100%; divergence is CHL | Shared ops fixtures agree device and desktop | `@SRS-EP-09` | **new** |
| Next stroke after ingest: pen-down → pixel p95 ≤30 ms; 0 samples dropped | Ingestion does not steal the next stroke's ink budget | `@SRS-EP-07` | **new** |

Adjacent (do not treat as this story's home; do not rewrite): `@SRS-EP-04` Pen ingest in
`device-tool-modes.feature`; `@SRS-EP-02` local ingest + 0 peer-picture paint in
`map-append-refresh.feature`. Undo / publish stay in `undo-ring.feature` / `one-way-sync.feature`.

### STORY-IN-027 — desktop `doc_change` applier

Home: existing `infini/features/tablet-sync/bdd/session-channels.feature` (**not rewritten**).

| AC | Scenario | Tag | Home |
|---|---|---|---|
| Inbound `doc_change` (`append_ink` or closed-set siblings) lands in VectorDocument; WorldLayer paints from the mirror, not `rebuildWithRmInk` | `append_ink from Epaper updates tree and WorldLayer` (`@future`); paint-from-tree also in `tree-backed-ink.feature` (`@SRS-IN-04`) | `@SRS-IN-07` | existing |
| Same `opId` twice → tree unchanged | Same `@future` scenario, second When/Then; also `tree-backed-ink` Duplicate change is idempotent | `@SRS-IN-07` | existing |
| last seq 12, `baseSeq` 15 → suspect + explicit resync + do not save | Sequence gap marks the mirror suspect | `@SRS-IN-07` | existing |
| Matching `doc_change` removes preview; 0 preview paths persisted/saved | Applied change replaces the preview | `@SRS-IN-07` | existing |
| Viewer session: Infini emits 0 document ops; only outbound document message is the epoch `doc_load` (IN-028) | Infini authors no document ops during a session (`@future`); No document message follows the load | `@SRS-IN-07` | existing |
| Device commit → mirror apply p95 ≤300 ms (SRS-IN-08) | Budget in SRS-IN-08 table; adjacent Then in `one-way-sync` “One change per committed op” (`@SRS-EP-08`) — no extra session-channels scenario added | `@SRS-IN-08` | existing (adjacent) |

`@future` stays on the two applier/viewer scenarios until `/qa` verify. Un-tag then, not now.

## Asks

1. `/dev` [STORY-EP-014](../stories/STORY-EP-014.md) — ingest + local paint. Do not ship the undo ring (EP-015) or publish queue (EP-020).
2. `/dev` [STORY-IN-027](../stories/STORY-IN-027.md) in parallel — inbound applier; WorldLayer source is the mirror.
3. Set each story `in-review` when its tagged scenarios pass → `/qa` verify. Do not mark `done`.

## Constraints

- Op types: **SRS-IN-09 transmit names** (`append_ink`, `create_smart_group`, `set_smart_transform`, `set_ink_scale_mode`, `reparent`, `remove_node`, `restore_snapshot`). Do not invent a third alias. SRS-IN-07 / IN-027 AC still say `set_smart_group_transform` / `remove` — treat those as the IN-09 names, not a second grammar.
- **0** peer round trips inside a gesture (pen-down → commit). **0** inbound document messages applied in that window.
- Desktop is not document authority. Do not revive `doc_snapshot` / `pickables` / `@SRS-IN-13`.
- Shared fixtures: `.docs/modules/infini/features/vector-document/fixtures/ops/` (and the mirrored copies under `epaper/tests` / `infini/tests`). Divergence → CHL, not a widened tolerance.
- Residue EP-007…011 / IN-020…026 is out of this walk.

## Out of scope

- Rewriting `undo-ring.feature`, `one-way-sync.feature`, `session-channels.feature`
- Deleting `@deprecated` infini originals
- EP-015 / EP-018 / EP-019 / EP-020 story status
- Application code in this QA walk (already complete)

## Gaps

None that cannot be tested. No CHL filed.
