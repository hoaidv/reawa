---
from: dev
to: qa
date: 2026-08-13
iter: iter-003
cc: [sm]
---

# Hand-off: Dev → QA — STORY-EP-016 on-device enclose recognition

## Pickup

| Story | Status | Writes |
|---|---|---|
| [STORY-EP-016](../stories/STORY-EP-016.md) | **in-review** | `epaper/` enclose + `create_smart_group` via `commitOp`; shared `enclose/` fixtures |

BDD home: [enclose-recognition.feature](../../../.docs/modules/epaper/features/ink-box/bdd/enclose-recognition.feature) (eight `@SRS-EP-10` scenarios — not rewritten).

**STOP for human RM2 drawing.** Non-synth epaper is running on the panel. Do **not** `--restore` xochitl. Do **not** flip [STORY-EP-017](../stories/STORY-EP-017.md) (still `draft`).

## What landed

On-device enclose at pen-up. Trigger is the tool **latched at pen-down** (Ink-box), not wire `intent`. Guards ported from `infini/src/document/recognizeEnclose.ts` (MIN 48 wu, ≥80% samples, skip already-grouped). Recognition is **not** an op; only `create_smart_group` is committed and queued. Failed guards → ordinary `append_ink`, 0 banner. No Infini recognition/membership source edits.

| Path | Role |
|---|---|
| `epaper/document/recognize_enclose.hpp` | Enclose guards + `commitOp(create_smart_group)`; Pen path is ordinary ink only (no membership) |
| `epaper/document/device_document.hpp` | `captureIds` detach; `layoutOffset` JSON on ink |
| `epaper/tabletcanvasitem.cpp` | Latch at pen-down; ingest **after** `flushPending()`; rasterize local tree after a successful enclose (not in `paint()`) |
| `.docs/.../fixtures/enclose/` + copies in `epaper/tests/fixtures/enclose/` and `infini/tests/fixtures/enclose/` | Shared corpus (byte-identical) |
| `epaper/tests/enclose_test.cpp` | Host map of all eight `@SRS-EP-10` scenarios |
| `infini/tests/enclose-fixtures.test.ts` | Same JSON through Infini recognizer (`armed` → `intent`); no `recognizeEnclose.ts` behaviour change |

## Behaviour (host)

1. Ink-box latched at pen-down → pen-up runs enclose. Guards pass → one `create_smart_group` (boundary + content with UV; local bounds at origin, transform carries world AABB). 0 desktop messages required; queue has `create_smart_group` only.
2. Pen latched → skip recognition; ordinary `append_ink`.
3. Live tool switch after pen-down does not change the latch.
4. Too small / no content → ordinary ink, 0 Smart Group, 0 banner.
5. Already-grouped ink skipped; remaining ungrouped ink still captures.
6. Ten consecutive successful encloses → 10 Smart Groups.
7. Undo (existing ring) restores the pre-create tree — one `commitOp` = one ring entry.
8. Session down: still create locally; in-memory queue from EP-015.

## Numbers (host this session — not device p95)

| Metric | Value | Bar |
|---|---|---|
| enclose → local create | host enclose_test (successful path) ≪ 500 ms | p95 ≤500 ms after pen-up ([SRS-EP-14](../../../.docs/modules/epaper/features/ink-box/srs-quality.md)); **not** claimed as device p95 |
| shared `enclose/` | 5 files, device + Infini 100% on verdict + fitted bounds | [SRS-EP-14] Cross-implementation |
| EP-013 re-run | dropped=0, paint-loop-hits=0, hit-test p95=1 µs | 0 drops |

## How to run

Host:

```bash
epaper/tests/run_device_document_test.sh
epaper/tests/run_latency_probe.sh
cd infini && npm test -- tests/enclose-fixtures.test.ts
```

Device (already deployed this session, **without** `RM_DOC_PROBE_SYNTH`):

```bash
cd epaper
./scripts/deploy-rm2.sh --build   # do not set RM_DOC_PROBE_SYNTH; do not --restore
```

USB `en7` / `10.11.99.12` → `10.11.99.1`. Epaper pid **5532**, app stayed up (no synth quit).

### Human drawing check (required stop)

1. Arm **Pen**, scribble some ink.
2. Arm **Ink-box** (right ToolChip tile), draw a **large** rectangle around that ink (shorter side ≳48 world units — a generous box on the panel).
3. Expect: the enclose stroke becomes the box boundary immediately; captured ink stays inside. No wait on Infini.
4. Tiny box or empty area → stroke stays ordinary ink; **no** error banner.
5. Mid-stroke tool switch should not change the meaning of that stroke.

## Asks

1. Map all eight [enclose-recognition.feature](../../../.docs/modules/epaper/features/ink-box/bdd/enclose-recognition.feature) scenarios to the host harness (and the human draw check for visibility / 0 banner).
2. Confirm shared `enclose/` fixtures: device vs Infini verdict + fitted bounds **100%**. Host + Infini copies are byte-identical to `.docs/.../fixtures/enclose/`.
3. Sign off **done** only if the eight scenarios hold, consecutive 10/10, and the human RM2 draw looks right. Host analog is not SRS-EP-14 device p95 by itself.
4. **STOP.** Do not flip [STORY-EP-017](../stories/STORY-EP-017.md). Leave epaper running.

`adlc audit` still lists `[SRS-EP-10]` / `[SRS-EP-14]` under orphan SRS because the scanner's `CODE_EXTS` omits `.hpp`/`.cpp`. Traces are on disk (`@implements [SRS-EP-10]` / `[SRS-EP-14]`). Not a CHL.

## Out of scope (unchanged)

- Draw-into membership (EP-017)
- Selection-create (EP-018 frozen)
- On-panel undo chrome (CHL-0010)
- EP-020 TCP / handshake
- Infini `recognizeEnclose.ts` behaviour
- Residue EP-007…011
