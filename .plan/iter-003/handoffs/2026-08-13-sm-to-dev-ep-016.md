---
from: sm
to: dev
date: 2026-08-13
iter: iter-003
cc: [qa]
---

# Hand-off: SM → Dev — STORY-EP-016 on-device enclose (after QA walk)

## Context

EP-014/015 **done**. Human stop after this story: leave a **non-synth** epaper binary on RM2
for drawing. Do not start EP-017.

## Pickup

| Lane | Story | Status | Writes |
|---|---|---|---|
| **C** | [STORY-EP-016](../stories/STORY-EP-016.md) | **ready** | `epaper/` enclose recognition + `create_smart_group` via `commitOp` |

## Lane C — EP-016

- Logic: `ink-box/srs-logic.md` `[SRS-EP-10]` enclose only (not draw-into membership)
- Quality: `[SRS-EP-14]` box visible p95 ≤500 ms after pen-up; 0 peer messages
- BDD: `ink-box/bdd/enclose-recognition.feature` `@SRS-EP-10`
- Port guards from `infini/src/document/recognizeEnclose.ts` (MIN 48 wu, ≥80% samples, skip
  already-grouped). Device trigger is **Ink-box armed at pen-down**, not wire `intent`.
- Commit via **`commitOp`** (`create_smart_group`) so undo (EP-015) gets one ring entry.
- Recognition is **not** an op. Only `create_smart_group` is queued.
- Failed guards → ordinary ink, 0 banner.
- Shared fixtures `enclose/`: add under `.docs/modules/infini/features/vector-document/fixtures/enclose/`
  and copies in `epaper/tests/fixtures/enclose/` + `infini/tests/fixtures/enclose/` (same pattern as
  `ops/`). Disagreement is a CHL, not a widened tolerance.
- Rasterize the local tree so the box is visible (existing `rasterizeVectors`).

## Constraints

- Do not edit `infini/src` except adding **shared fixture JSON** if needed for parity tests
- Do not ship draw-into membership (EP-017)
- Do not ship selection-create (EP-018 frozen)
- No peer round trip inside the gesture
- Do not put recognition in `paint()`; keep ingest after `flushPending()`
- Op names: SRS-IN-09 `create_smart_group` only

## After code

Set `in-review`. Deploy **without** `RM_DOC_PROBE_SYNTH` so the human can draw. Do not
`--restore` xochitl. Do not flip EP-017.
