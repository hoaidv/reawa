---
from: qa
to: sm
date: 2026-08-13
iter: iter-003
cc: [dev, pm]
verdict: PASS
---

# Hand-off: QA → SM — STORY-EP-014 done (device tree + ingest)

## Pickup

| Story | Status | Writes |
|---|---|---|
| [STORY-EP-014](../stories/STORY-EP-014.md) | **done** | story notes + this handoff — no `CHL-*` / `DEF-*` |

Lane C lock held: no EP-015 `ready`; no undo/publish; no `infini/` edits; no residue EP-007…011.

## Host (re-run this session)

`epaper/tests/run_device_document_test.sh` — **PASS**.

```
ingest pen-up→node n=40 p50=7875ns p95=14542ns p99=21583ns (p95=15us)
device_document_test: checks passed
```

`epaper/tests/run_latency_probe.sh` — **PASS**. dropped=0, paint-loop-hits=0, hit-test p95=1 µs.

Host analog is not the gate.

## Device (RM2 `10.11.99.1` via USB `en7`)

Earlier default-route ping went out Wi-Fi (`10.0.245.85`, TTL exceeded). Bound to `10.11.99.12` the tablet answers. Stale `known_hosts` ED25519 for `10.11.99.1` was refreshed (USB gadget key rotation). Synth protocol from the Dev handoff (`RM_INK_TRACE=1 RM_DOC_PROBE=1 RM_DOC_PROBE_SYNTH=1 ./scripts/deploy-rm2.sh --build`). xochitl restored after.

```
[ink-trace] arrival->flush n=82 p50=855us p95=1392us p99=3125us
[ink-trace] flush->swap: (no samples)
[doc-probe] fixture nodes=500 samples=50000 enabled=1 every_sample=0
[doc-probe] hit-test n=40 p50=14000ns p95=23249ns p99=23499ns (p95=23us)
[doc-probe] samples processed=840 dropped=0 paint-loop-hits=0
[doc-ingest] ink_nodes=40 applied=40 rejected=0 n=40 p50=160121ns p95=231245ns p99=246619ns (p95=231us)
```

| Bar | Result |
|---|---|
| 20 (synth: 40) finished strokes → Ink nodes | **ink_nodes=40** applied=40 rejected=0 |
| p95 after pen-up ≤50 ms | **231 µs** |
| arrival→flush vs EP-013 band (~798–1517 µs) | p95=**1392 µs** — 21× under 30 ms |
| dropped=0 | **0** |
| paint-loop-hits=0 | **0** |
| hit-test p95 ≤100 ms | **23 µs** |

## ingest-stroke.feature → results

| Scenario | Tag | Host | Device |
|---|---|---|---|
| Finished strokes become Ink nodes with no session | `@SRS-EP-07` | **PASS** — 20 Ink nodes; pen-up→node p95=15 µs | **PASS** — `ink_nodes=40` p95=231 µs; paint source is local `rasterizeVectors`; no inbound peer picture |
| Selection armed does not ingest ink | `@SRS-EP-07` | **PASS** code path — press/move/release skip `beginStroke` / `endStroke` when `m_toolMode == "selection"` | synth is Pen; same `ingestPoint` skip |
| Digitizer channels survive on the stored node | `@SRS-EP-09` | **PASS** — pressure, tiltX/Y, distance, timestamp, extras | same `ingestCurrentStroke` path |
| Shared ops fixtures agree device and desktop | `@SRS-EP-09` | **PASS** — `append_ink` without frame → `bad_parent:frm_1`; then frame+append+primitive trees match | same `DeviceDocument::applyOp` |
| Ingestion does not steal the next stroke's ink budget | `@SRS-EP-07` | **PASS** analog — dropped=0, paint-loop-hits=0 | **PASS** — arrival→flush p95=1392 µs; dropped=0; paint-loop-hits=0 |

## Spot-check

- Ingest runs **after** `flushPending()` in `endStroke()` — not between a sample and its pixel, **not** in `paint()` (`paint()` blits `QImage` + selection chrome only).
- Undo ring / publish queue **not** shipped (no `UndoRing` / `publish_queue` / `doc_change` enqueue). EP-015 remains `draft`.
- `infini/` not edited.

`adlc audit` still lists `[SRS-EP-07]` / `[SRS-EP-09]` as orphan SRS (scanner `CODE_EXTS` omits `.hpp`/`.cpp`). Traces are on disk. Not a CHL.

## Asks

1. [STORY-EP-014](../stories/STORY-EP-014.md) is **`done`**. Flip [STORY-EP-015](../stories/STORY-EP-015.md) `draft` → `ready` when you choose — **QA did not flip it**.
2. Do not start `/dev` on the undo ring until that flip. EP-015 ships the ring with **no chrome** (CHL-0010).
3. Do not start IN-028 / EP-016+ / residue EP-007…011.

## Out of scope (unchanged)

- Undo ring (EP-015) / publish queue + handshake (EP-020)
- Enclose / membership / manipulation (EP-010 / EP-011)
- Infini tree
- Residue EP-007…011
- Design packages / `srs-ui.md`
