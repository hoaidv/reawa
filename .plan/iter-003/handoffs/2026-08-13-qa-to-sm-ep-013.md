---
from: qa
to: sm
date: 2026-08-13
iter: iter-003
cc: [dev, pm]
verdict: PASS
---

# Hand-off: QA → SM — STORY-EP-013 done (device pass)

## Pickup

| Story | Status | Writes |
|---|---|---|
| [STORY-EP-013](../stories/STORY-EP-013.md) | **done** | story notes only — no `CHL-*` / `DEF-*` |

Lane B lock held: no EP-012 / EP-014 / design-package / BDD / `srs-logic.md` edits.

## Host (re-run)

`epaper/tests/run_latency_probe.sh` — **PASS**. Fixture 500 / 50k. dropped=0,
paint-loop-hits=0, ingest-path hit-test p95=625 ns. Host analog is not the gate.

## Device (RM2 `10.11.99.1` reachable)

Synth protocol from the Dev handoff (`RM_INK_TRACE=1 RM_DOC_PROBE=1 RM_DOC_PROBE_SYNTH=1`
`./scripts/deploy-rm2.sh --build`). xochitl restored after.

```
[ink-trace] arrival->flush n=80 p50=813us p95=1298us p99=3102us
[ink-trace] flush->swap: (no samples)
[doc-probe] fixture nodes=500 samples=50000 enabled=1 every_sample=0
[doc-probe] hit-test n=40 p50=12499ns p95=22249ns p99=24249ns (p95=22us)
[doc-probe] samples processed=840 dropped=0 paint-loop-hits=0
```

| Bar | Result |
|---|---|
| arrival→flush vs 798 µs band | p95=1298 µs — between 2026-08-09 p95 (798) and p99 (1517); 23× under 30 ms |
| pen-down → pixel ≤30 ms | instrumented path 1.3 ms; eye not re-run (synth); `flush->swap` empty |
| hit-test p95 ≤100 ms | **22 µs** |
| dropped=0 | **0** |
| paint-loop-hits=0 | **0** |

Paint-loop hygiene (spot-check): `TabletCanvasItem::paint()` blits `QImage` only;
probe is gated in `ingestPoint` (`RM_DOC_PROBE` / `RM_DOC_PROBE_SYNTH`).

## Asks

1. Ungate W9 / flip [STORY-EP-014](../stories/STORY-EP-014.md) `draft` → `ready` when you choose — **QA did not flip it**.
2. Do not start `/dev` on REQ-04 implement until that flip.
3. `/designer` STORY-EP-012 remains the parallel lane; do not mix.

## Out of scope (unchanged)

- DeviceDocument tree, undo, sync publisher
- `device-document/srs-logic.md` / CHL-0009
- Design package
- Residue EP-007…011 / IN-020…026
