---
from: dev
to: qa
date: 2026-08-14
iter: iter-003
---

# Hand-off: Dev → QA — EP-025

## What shipped

Refresh-class split ([ADR-0019](../../../.docs/adr/ADR-0019-selection-chrome-layers.md) /
[CHL-0017](../challenges/CHL-0017-selection-chrome-layers.md)):

| Layer | What |
|---|---|
| **CanvasLayer** (`TabletCanvas`) | Document `m_image` + live move/resize ink. Pen. Chrome `update()` with no rect **removed**. |
| **ToolCanvasLayer** (`ToolCanvas`) | Marquee, lasso polyline, settled dotted AABB. Tight dirty rects (last segment / old∪new). `EPScreenModeItem` **Mono** when the enum exists; else tight-bbox fallback. Does not blit the document. |
| **ToolLayer** (QML) | 6/8 handles, ink-scale chip. Enclose / ToolChip unchanged. Visual only — gestures stay on CanvasLayer. |

No enclose / membership / `set_smart_transform` edits.

## Host tests

`epaper/tests/run_device_document_test.sh` — **PASS** (device_document, enclose, membership, surround_create, debug_log, manipulate).

## Asks (on-panel)

Redeploy `epaper`. HUD may show `| mono` if Mono attached.

1. `sel_freeform` lasso follows the pen without full-panel flash; ink with `pen` still feels like EP-013.
2. `sel_rect` marquee same.
3. Pen-up: lasso gone, tight AABB + 6 anchors (or 8 handles + chip on a SmartGroup); Enclose still QML.
4. EP-018 Enclose and EP-019 move/resize still work.

## Next

`/qa` verify EP-025.
