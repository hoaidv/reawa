---
from: qa
to: sm
date: 2026-08-13
iter: iter-003
cc: [dev, pm]
verdict: PASS
---

# Hand-off: QA → SM — STORY-EP-015 done (device snapshot undo ring)

## Pickup

| Story | Status | Writes |
|---|---|---|
| [STORY-EP-015](../stories/STORY-EP-015.md) | **done** | story notes + this handoff — no `CHL-*` / `DEF-*` |

Lane C lock held: no on-panel undo chrome (CHL-0010); no EP-020 handshake; no `infini/` edits; no residue EP-007…011; **EP-016 remains `draft`**.

BDD: [undo-ring.feature](../../../.docs/modules/epaper/features/device-document/bdd/undo-ring.feature) (eight `@SRS-EP-07` — not rewritten).

## Host (re-run this session)

`epaper/tests/run_device_document_test.sh` — **PASS**.

```
ingest pen-up→node n=40 p50=16375ns p95=25291ns p99=28917ns (p95=25us)
undo request→restored n=20 p50=1459ns p95=1958ns (p95=2us host analog, bar 500ms)
device_document_test: checks passed
```

`epaper/tests/run_latency_probe.sh` — **PASS**. dropped=0, paint-loop-hits=0, hit-test p95=1 µs.

Host analog is **not** SRS-EP-13 device undo p95. No RM2 undo run (no on-panel control to exercise). Do not HOLD for missing device undo p95.

## undo-ring.feature → host harness

| Scenario | Tag | Host |
|---|---|---|
| Structural op pushes a snapshot before apply | `@SRS-EP-07` | **PASS** — `test_undo_structural_op_pushes_pre_op_snapshot`: depth 1; newest entry snapshot equals pre-op tree; raw `applyOp` does not push; rejected `commitOp` leaves ring/queue/tree unchanged |
| Undo restores the prior tree exactly | `@SRS-EP-07` | **PASS** — `test_undo_restores_prior_tree_exactly`: `snapshotString` bit-exact; `treesEqualGeom` ±1 world unit (0 divergent nodes); empty-ring undo is a no-op |
| One completed gesture is one undo entry | `@SRS-EP-07` | **PASS** — `test_undo_one_gesture_one_entry`: 40 `previewManipulationFrame`s do not push; one `commitOp` at release → depth +1; one undo restores pre-move transform |
| Viewport tool and selection do not push undo | `@SRS-EP-07` | **PASS** — `test_undo_viewport_tool_selection_do_not_push`: pan / tool / selection leave depth 0 and empty queue |
| Ring overflow drops the oldest entry | `@SRS-EP-07` | **PASS** — `test_undo_ring_overflow_drops_oldest`: 20 then 21st; depth stays 20; oldest pre-first snapshot gone |
| Undo requested mid-gesture is deferred | `@SRS-EP-07` | **PASS** — `test_undo_mid_gesture_is_deferred`: latched, tree uncorrupted during flight; undo runs after commit; last queue op is `restore_snapshot` |
| Undo publishes as a change | `@SRS-EP-07` | **PASS** — `test_undo_publishes_restore_snapshot`: queued `restore_snapshot.document` equals restored tree (`stringify` match). Desktop-mirror analog — no USB / Infini edits |
| An accepted document load clears the ring | `@SRS-EP-07` | **PASS** — `test_undo_accepted_doc_load_clears_ring`: ring empty; undo is no-op; pre-load nodes unreachable |

## Spot-check

- Ingest still **after** `flushPending()` in `endStroke()`; then `ingestCurrentStroke()` → `commitOp`. Not in `paint()` (`paint()` blits `QImage` + selection chrome only).
- Undo is not in `paint()`. Ring lives on `DeviceDocument`; `beginGesture` at pen-down; `abortGesture` after ingest.
- No undo button / fourth ToolChip: `Main.qml` model is Selection · Pen · Ink-box (3); chip width `64 * 3`. CHL-0010 held.
- `infini/` not edited (`git diff HEAD -- infini` empty).
- Op name is **`restore_snapshot` only** (`undoNow` sets `rst.type = "restore_snapshot"`; tests assert that name). In-memory queue — no TCP / handshake (EP-020).

`adlc audit` still lists `[SRS-EP-07]` under orphan SRS because the scanner's `CODE_EXTS` omits `.hpp`/`.cpp`. Traces are on disk (`@implements [SRS-EP-07]`). Not a CHL.

## Asks

1. [STORY-EP-015](../stories/STORY-EP-015.md) is **`done`**. Do **not** let QA flip [STORY-EP-016](../stories/STORY-EP-016.md) — it stays `draft` until SM opens W10.
2. Do not start W10 / enclose / membership / residue EP-007…011 / IN-020…026.
3. Do not invent on-panel undo chrome (CHL-0010). Do not start EP-020 handshake.

## Out of scope (unchanged)

- On-panel undo chrome (CHL-0010)
- EP-020 TCP / handshake / `doc_load` accept UI
- Enclose (EP-016) / Infini edits
- Residue EP-007…011
- Device undo p95 (no on-panel undo to exercise on RM2)
