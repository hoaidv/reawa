---
from: qa
to: dev
date: 2026-08-13
iter: iter-003
cc: [sm]
verdict: READY
---

# Hand-off: QA → Dev — STORY-EP-015 BDD walk (review only)

## Context

Author-mode coverage walk on [STORY-EP-015](../stories/STORY-EP-015.md) (`status: ready`).
Feature file already existed; **not rewritten**. No chrome scenarios added
([CHL-0010](../challenges/CHL-0010-undo-vs-selection-create-chrome.md) deferred).
Story status left **`ready`** — Dev has not started.

Home: [undo-ring.feature](../../../.docs/modules/epaper/features/device-document/bdd/undo-ring.feature)
(`@SRS-EP-07`, eight scenarios). Parent: [SRS-EP-07](../../../.docs/modules/epaper/features/device-document/srs-logic.md)
gesture-commit + undo. Budgets: [SRS-EP-13](../../../.docs/modules/epaper/features/device-document/srs-quality.md).

## Coverage verdict

**READY-FOR-DEV.** Every EP-015 AC has a matching `@SRS-EP-07` scenario. No Gherkin added or rewritten.

## AC ↔ scenario

| # | AC (story) | Scenario | Tag | Notes |
|---|---|---|---|---|
| 1 | Structural op through undo-aware path commits → ring gained exactly one pre-op snapshot **and** one undo restores that tree exactly (geometry ±1 world unit) | **Structural op pushes a snapshot before apply** + **Undo restores the prior tree exactly** | `@SRS-EP-07` | Split across two scenarios (push vs restore). Empty-ring second undo is a no-op (SRS). Geometry ±1 is [SRS-EP-13](../../../.docs/modules/epaper/features/device-document/srs-quality.md) exactness — assert in tests; Gherkin says “exactly” |
| 2 | Move that rendered many intermediate frames, released → ring gained exactly one entry; one undo reverts that gesture | **One completed gesture is one undo entry** | `@SRS-EP-07` | Intermediate frames do not push |
| 3 | 20 structural ops, 21st push → depth stays 20; oldest gone | **Ring overflow drops the oldest entry** | `@SRS-EP-07` | Depth 20 |
| 4 | Only pan, tool, or selection-state changes → ring depth unchanged | **Viewport tool and selection do not push undo** | `@SRS-EP-07` | Not document state |
| 5 | Undo requested mid-gesture → gesture not corrupted; undo applies after commit | **Undo requested mid-gesture is deferred** | `@SRS-EP-07` | Latch, then run after commit |
| 6 | Live session, undo runs → `restore_snapshot` change queued; p95 request → restored panel ≤500 ms | **Undo publishes as a change** | `@SRS-EP-07` | “Desktop mirror converges” = device **queues** `restore_snapshot`; Infini already applies (IN-027). Do not require USB or Infini edits. p95 ≤500 ms is SRS-EP-13 Undo latency — measure in device/unit tests; not a ninth scenario |
| 7 | Accepted `doc_load` completes → ring empty; undo cannot reach pre-load tree | **An accepted document load clears the ring** | `@SRS-EP-07` | Device hook, unit-testable. Full handshake is EP-020 / IN-028 |

No fourth ToolChip / undo-button scenario. No on-panel chrome.

## Asks

1. `/dev` [STORY-EP-015](../stories/STORY-EP-015.md) — snapshot undo ring in `epaper/` only.
2. Implement so all eight `@SRS-EP-07` scenarios in `undo-ring.feature` can pass.
3. Set the story `in-review` when tests pass → `/qa` verify. **Do not** mark `done`.

## Constraints

- **No on-panel undo chrome** (CHL-0010). No fourth ToolChip slot, no properties panel, no undo button.
- Do not edit `infini/`. Do not start EP-016 / EP-018 / EP-020 / IN-028.
- Queue `restore_snapshot` locally; do not wait on a USB round trip for the ring to be correct.
- `doc_load` clears the ring as a device hook (empty publish queue in the Given). Handshake UI/TCP is EP-020.
- Op name: `restore_snapshot` (SRS-IN-09 transmit set). No third alias.
- **0** peer round trips inside a gesture. Do not put undo work in `paint()`.
- Residue EP-007…011 is out of this story.

## Out of scope

- Rewriting `undo-ring.feature`
- Flipping EP-015 to `done` or `in-progress` (Dev sets `in-progress` / `in-review`)
- Application code in this QA walk
- Enclose (EP-016), handshake (EP-020), Infini edits

## Gaps

None. No CHL filed.

`adlc audit`: orphan `[SRS-EP-07]` (and sibling EP-*.hpp) is expected — scanner `CODE_EXTS` omits `.hpp`/`.cpp`. Not a CHL.
