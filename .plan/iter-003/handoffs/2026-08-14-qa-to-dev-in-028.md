---
from: qa
to: dev
date: 2026-08-14
iter: iter-003
cc: [sm]
verdict: READY
---

# Hand-off: QA → Dev — STORY-IN-028 BDD ready

## Context

W12 ingest/desktop lane. Author-mode BDD for handshake-gated `doc_load` (drain then load).
Story stays **ready**. Sibling [STORY-EP-020](../stories/STORY-EP-020.md) is owned by the
other QA walk — not implemented here. ADR-0019 / CHL-0018 does not gate W12.

## Coverage

Home: [handshake-doc-load.feature](../../../.docs/modules/infini/features/tablet-sync/bdd/handshake-doc-load.feature).

Transmit names from [SRS-IN-09](../../../.docs/modules/infini/features/vector-document/srs-data.md)
and ADR-0015: `hello`, `drain_ack`, `doc_change`, `queue_empty`, `doc_load`, `load_ack`,
`viewport`. Op envelopes `{ opId, type, payload }`. Do not invent aliases (`doc_snapshot` is
retired).

| AC | Scenario | Tag |
|---|---|---|
| queued > 0 → `drain_ack` first, apply `doc_change` in seq, `doc_load` only after `queue_empty` | Drain then doc_load when queued is greater than zero | `@SRS-IN-07` |
| After load, 0 further outbound document messages (viewport-only down) | Zero outbound document messages after the load | `@SRS-IN-07` `@SRS-IN-08` |
| Reconnect does not push a document reflexively — handshake (drain then load) | Reconnect runs the handshake instead of a reflexive document push | `@SRS-IN-07` `@SRS-IN-08` |
| Orientation change or Infini-side action → 0 `doc_load` / `doc_snapshot` | Orientation change and Infini-side actions send zero doc_load | `@SRS-IN-07` |
| Retired `doc_snapshot`, `pickables`, `tool_intent` are not emitted | Retired snapshot pickables and tool_intent are not emitted | `@SRS-IN-07` |

Adjacent (do not rewrite): `session-channels.feature` already has a thinner handshake /
no-document-after-load pair. IN-028 AC live in the new file.

## Asks

1. `/dev` implement [STORY-IN-028](../stories/STORY-IN-028.md) — desktop drain then `doc_load`.
2. Do not regress viewport publish (already shipped).
3. Set `in-review` when tagged scenarios pass → `/qa` verify. Do not mark `done`.

## Constraints

- Never fork SRS-IN-09 op / envelope names.
- Do not implement EP-020 in this lane.
