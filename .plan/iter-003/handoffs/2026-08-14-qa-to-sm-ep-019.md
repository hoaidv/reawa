---
from: qa
to: sm
date: 2026-08-14
story: STORY-EP-019
verdict: HOLD
---

# Hand-off: QA → SM — STORY-EP-019 HOLD (need on-panel confirm)

Host suite **PASS** (`run_device_document_test.sh`, including `manipulate_test`). Story stays
**in-review**. Unlike EP-024, there is **no human RM2 confirm** for live manipulation this session.

## `@SRS-EP-11` map

| Scenario | Host | Device |
|---|---|---|
| Hit topmost + chrome ≤100 ms (rect / freeform) | not asserted | **need confirm** |
| Four-tool chip; Enclose not a tile | code/Spec spot-check PASS (ADR-0018 actions extra, not tools) | **need confirm** |
| Drag real ink; one `set_smart_transform`; commit = preview | **PASS** `test_move_commit_equals_preview` | **need confirm** live follow |
| ≥5 Hz, stall ≤200 ms, 0 full-panel | not measured | **need confirm** |
| fixedInk UV + sample size | **PASS** | **need confirm** |
| withBounds scales; boundary follows | **PASS** | **need confirm** |
| Invert handle → non-negative | **PASS** | **need confirm** |
| LOD &lt; 96 du unavailable | **PASS** `test_lod` | **need confirm** banner |
| Deselect 0 residue | not on host | **need confirm** |
| Link drop mid-gesture | not on host | skip if no session drop |
| Capability descriptor, no kind in router | **PASS** `test_descriptor` / `test_router_no_kind_branch` | — |

EP-018 enclose tests still green (no regress).

## Ask human

On RM2, with `sel_rect` and `sel_freeform`: select, move, both resize modes, inverted handle,
empty-canvas deselect, zoom/small box LOD banner. Reply **confirm** → QA will set **done**.
