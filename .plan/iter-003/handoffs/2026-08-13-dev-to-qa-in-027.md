---
from: dev
to: qa
date: 2026-08-13
iter: iter-003
cc: [sm]
---

# Hand-off: Dev → QA — STORY-IN-027 desktop doc_change applier

## Pickup

| Story | Status | Writes |
|---|---|---|
| [STORY-IN-027](../stories/STORY-IN-027.md) | **in-review** | `infini/` inbound `doc_change` → VectorDocument; WorldLayer paints the **mirror** |

Lane D only. Did not edit `epaper/` / `Epaper/`, design packages, or `srs-ui.md`.

## What landed

- **Applier:** `TabletSession.receiveDocChange` (`infini/src/session/TabletSession.ts`) — seq/`baseSeq` gap detection, idempotent `opId`, unknown-op → suspect + resync flag, preview replaced by the matching node.
- **Tree ops:** `VectorDocument.applyOp` accepts wire names `append_ink`, `create_smart_group`, `set_smart_group_transform`, `set_ink_scale_mode`, `reparent`, `remove`, `restore_snapshot` (`infini/src/document/VectorDocument.ts`).
- **Paint source:** `TabletSession.paintMirror()` — WorldLayer from `flattenDrawables` + transient `preview:*` paths. `CanvasStage.rebuildWithRmInk` is withdrawn as the document source.
- **Viewer:** Infini emits **0** document ops (`emitLocalStructureOp` refuses; `IpcRmTransport.sendDocOp` is a no-op). No `doc_snapshot` push. **`doc_load` not shipped** (IN-028).
- **Save guard:** `mirrorSuspect` / `canSave()` / `serializeInfiniSvg` throws `refuse_save_suspect_mirror`.

## Tests

```bash
cd infini && npm test
```

80 passed, including `tests/doc-change-applier.test.ts` (`@SRS-IN-07` apply / idempotent / gap / preview-replace / viewer-silent / p95 apply budget).

Host apply p95 of 40 `append_ink` ops is well under 300 ms (in-process; not a USB-link measurement).

## Asks

1. Walk `session-channels.feature` `@SRS-IN-07` apply / gap / preview-replace scenarios against the applier tests.
2. Confirm WorldLayer source is the mirror (no `rm-live-*` / `rebuildWithRmInk` committed paths).
3. Confirm 0 outbound document messages (viewport still legal). Do **not** require `doc_load` — that is IN-028.
4. Sign off **done** only if AC hold. Miss → `CHL-*`, do not start IN-028.

## Out of scope (unchanged)

- Handshake-gated `doc_load` (IN-028)
- Device tree / publish (EP-014 lane C)
- Residue EP-007…011 / IN-020…026 as live acceptance
- Design package / `srs-ui.md`
