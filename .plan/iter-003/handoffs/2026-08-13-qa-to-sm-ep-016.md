---
from: qa
to: sm
date: 2026-08-13
iter: iter-003
cc: [dev, pm]
verdict: HOST-PASS / WAIT-HUMAN-DRAW
---

# Hand-off: QA → SM — STORY-EP-016 host-pass; wait human RM2 draw

## Pickup

| Story | Status | Writes |
|---|---|---|
| [STORY-EP-016](../stories/STORY-EP-016.md) | **in-review** (not `done`) | this handoff + story QA note — no `CHL-*` / `DEF-*` |
| [STORY-EP-017](../stories/STORY-EP-017.md) | **draft** (untouched) | — |

Lane C lock held: no membership (EP-017); no selection-create (EP-018 frozen); no Infini logic edits; no `--restore` xochitl; no residue EP-007…011.

BDD: [enclose-recognition.feature](../../../.docs/modules/epaper/features/ink-box/bdd/enclose-recognition.feature) (eight `@SRS-EP-10` — not rewritten).

## Verdict

**HOST-PASS / WAIT-HUMAN-DRAW.** Host harnesses green. Human RM2 drawing is the remaining gate (SM stop). Story stays **`in-review`**. Do not flip `done`. Do not file `DEF-*` for the pending eye check.

## Host (re-run this session)

`epaper/tests/run_device_document_test.sh` — **PASS**.

```
ingest pen-up→node n=40 p50=21166ns p95=26792ns p99=30000ns (p95=27us)
undo request→restored n=20 p50=1500ns p95=2250ns (p95=2us host analog, bar 500ms)
device_document_test: checks passed
enclose_test: checks passed
```

`epaper/tests/run_latency_probe.sh` — **PASS**. dropped=0, paint-loop-hits=0, hit-test p95=1 µs.

`cd infini && npm test -- tests/enclose-fixtures.test.ts` — **PASS**. 6 tests / 1 file (5 fixtures + byte-identical copies).

Host analog is **not** SRS-EP-14 device visibility p95. Do not claim ≤500 ms on-panel from this run.

## enclose-recognition.feature → host harness

| Scenario | Tag | Host |
|---|---|---|
| Successful enclose commits Smart Group immediately and locally | `@SRS-EP-10` | **PASS** — `test_successful_enclose_local`: `create_smart_group` committed; queue +1; 0 desktop messages required (host analog); enclose stroke role `boundary`; captured ink role `content` with `layoutOffset` UV in (0,1); local bounds origin + transform carries world AABB (40,40,120×120). Visibility p95 is host analog (`ns ≤ 500 ms`); **WAIT-HUMAN-DRAW** for panel |
| Stroke drawn with Pen armed skips recognition | `@SRS-EP-10` | **PASS** — `test_pen_armed_skips`: `ordinary_ink` / `pen_armed`; 0 Smart Groups; stroke stays Ink |
| Tool switched mid-stroke does not change what the stroke means | `@SRS-EP-10` | **PASS** — `test_latch_ignores_live_tool`: live `applyToolSwitch("pen")` after Ink-box latch still creates; `uiTool()` is `pen`. **WAIT-HUMAN-DRAW** for mid-stroke chip switch |
| Failed guards leave ordinary ink | `@SRS-EP-10` | **PASS** — `test_failed_guards_ordinary_ink`: `too_small` and `no_content` → 0 Smart Groups, ordinary Ink. No error-banner QML/C++ exists (0 banner analog). **WAIT-HUMAN-DRAW** for no banner on panel |
| Ink already inside a Smart Group is skipped | `@SRS-EP-10` | **PASS** — `test_already_grouped_skipped`: `already` not in new children; `free` + enclose stroke captured |
| Enclose works with the session down | `@SRS-EP-10` | **PASS** — `test_session_down_queues_create`: local create; queue size 1; op type `create_smart_group` only (not `recognize_enclose`) |
| Consecutive encloses stay correct | `@SRS-EP-10` | **PASS** — `test_ten_consecutive_encloses`: 10 / 10 Smart Groups; undo depth 10 (CHL-0007 analog) |
| Undo restores the pre-create tree | `@SRS-EP-10` | **PASS** — `test_undo_restores_pre_create`: one `create_smart_group` ring entry; `snapshotString` bit-exact after undo |

Shared `enclose/` (5 files): device `test_shared_enclose_fixtures` + Infini `enclose-fixtures.test.ts` **100%** on verdict + fitted bounds. Docs / epaper / infini copies byte-identical.

## Spot-check (no source edits)

- **No draw-into membership shipped.** Pen latch → `appendOrdinaryInk` only (`recognize_enclose.hpp`). `membership` / `tryDrawIntoMembership` not called from epaper. EP-017 still `draft`.
- **Ingest after `flushPending()`.** `endStroke()`: pixels first, then `ingestCurrentStroke()` → `ingestStrokeAtPenUp`.
- **Recognition not in `paint()`.** `paint()` blits `QImage` + selection chrome only. Successful enclose sets `m_needEncloseRasterize`; rasterize runs after ingest in `endStroke()`.
- **`create_smart_group` only.** Queue back is `create_smart_group`; explicit `!= "recognize_enclose"`. Recognition is not an op.
- **0 banner code** in epaper QML/C++ (failed-guard path is ordinary ink, no UI).

`adlc audit` still lists `[SRS-EP-10]` / `[SRS-EP-14]` under orphan SRS because the scanner's `CODE_EXTS` omits `.hpp`/`.cpp`. Traces are on disk (`@implements [SRS-EP-10]` / `[SRS-EP-14]`). Not a CHL.

## Device (RM2 — ping only, no restart)

USB `en7` (`10.11.99.12` → `10.11.99.1`). Ping **yes**. `ssh` Reawa key → `pgrep -a epaper` → **`5532 ./epaper`** (same pid as Dev deploy). App left running. Did **not** `--restore` xochitl. Did **not** redeploy.

### Human drawing check (remaining gate)

1. Arm **Pen**, scribble some ink.
2. Arm **Ink-box** (right ToolChip tile), draw a **large** rectangle around that ink (shorter side ≳48 world units).
3. Expect: enclose stroke becomes the box boundary immediately; captured ink stays inside. No wait on Infini.
4. Tiny box or empty area → stroke stays ordinary ink; **no** error banner.
5. Mid-stroke tool switch should not change the meaning of that stroke.

## Asks

1. [STORY-EP-016](../stories/STORY-EP-016.md) stays **`in-review`**. After the human RM2 draw looks right, SM (or a follow-up `/qa`) may flip **`done`**. Do not treat host analog as device p95.
2. Do **not** flip [STORY-EP-017](../stories/STORY-EP-017.md). Leave non-synth epaper on RM2. Do not `--restore` xochitl until the human is done drawing.
3. Do not start membership / EP-018 / residue EP-007…011.

## Out of scope (unchanged)

- Draw-into membership (EP-017)
- Selection-create (EP-018 frozen)
- On-panel undo chrome (CHL-0010)
- EP-020 TCP / handshake
- Infini `recognizeEnclose.ts` behaviour
- Residue EP-007…011
- Device enclose visibility p95 (human draw)
