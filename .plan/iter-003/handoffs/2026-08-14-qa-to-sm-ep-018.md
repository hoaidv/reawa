---
from: qa
to: sm
date: 2026-08-14
iter: iter-003
verdict: HOLD
---

# Hand-off: QA → SM — EP-018

## Verdict

**HOLD** — host mapped suite **PASS**. On-panel checklist from
[sm-to-qa](./2026-08-14-sm-to-qa-ep-018.md) is **not signed** in this session. Story stays
`in-review`. No `DEF-*`. W11b stays gated.

## Host (PASS)

`epaper/tests/run_device_document_test.sh` — all green, including `surround_create_test`:

| Scenario | Mapped check |
|---|---|
| Rect ≥80% / graze not selected | `selectByRect` in + out + graze |
| Freeform ≥80% inside polyline | `selectByFreeform` in + out |
| Enclose create / refuse / later sibling / open path / undo | `createSmartGroupFromSelection` cases |
| SmartGroup in selection | `reason == smartgroup_in_selection` |

## Spec spot-check (read-only)

- [UI-EP-03](../design/selection-enclose-chrome/ui-spec.md): four-chip + overlay Enclose.
- `Main.qml` ToolChip: `sel_rect`, `sel_freeform`, `pen`, `ink_box` — Enclose not a tile.

Chrome-while-dragging and leftover-rect-after-Enclose are **panel-only**.

## On-panel still required (human)

Redeploy latest `epaper/build/bin/epaper`, then:

1. ≥80% membership; graze does not select; HUD `sel=`
2. Settled chrome after a closed lasso; tap clears
3. Enclose: box + no stray dashed rect + `[enclose] outcome=created`; refuse copy
4. Four tools only
5. Occasional `swtcon fallen behind` is not a fail unless chrome stays stale for seconds

## Next

Human panel PASS → `/qa` again (or tell SM) to flip EP-018 `done`. FAIL → `/dev`. Not `/pm` yet.
