---
from: qa
to: sm
date: 2026-08-13
iter: iter-003
cc: [dev, pm]
verdict: PASS
---

# Hand-off: QA → SM — STORY-IN-027 done (desktop doc_change applier)

## Pickup

| Story | Status | Writes |
|---|---|---|
| [STORY-IN-027](../stories/STORY-IN-027.md) | **done** | BDD un-tag only; no `CHL-*` / `DEF-*` |

Lane D lock held: no `epaper/` / EP-014 / IN-028 `ready` / design-package / `srs-ui.md`.

## Host

`cd infini && npm test` — **PASS**. 13 files, **80 passed**. Includes
`tests/doc-change-applier.test.ts` (`@SRS-IN-07` apply / idempotent / gap / preview-replace /
viewer-silent / unknown-op; `@SRS-IN-08` p95 apply budget).

## AC → tests + BDD

| AC | Unit test | BDD |
|---|---|---|
| Inbound `doc_change` lands in VectorDocument; WorldLayer paints the mirror, not `rebuildWithRmInk` | `applies into VectorDocument and paints the mirror node`; transmit-ops cases (`create_smart_group` / `set_smart_group_transform` / `set_ink_scale_mode` / `reparent` / `remove` / `restore_snapshot`) | `append_ink from Epaper updates tree and WorldLayer` — **un-tagged `@future`** |
| Same `opId` twice → tree unchanged | `is idempotent on duplicate opId` | same scenario, second When/Then |
| last seq 12, `baseSeq` 15 → suspect + resync + do not save | `does not apply, requests resync, and refuses save` | `Sequence gap marks the mirror suspect` (already live) |
| Matching `doc_change` drops preview; 0 preview paths persisted/saved | `drops the preview path; the mirror node paints in its place` | `Applied change replaces the preview` (already live) |
| Viewer: 0 document ops; only outbound document message is epoch `doc_load` (IN-028) | `emits zero document ops for a viewer session` — 0 ops + viewport still legal. **`doc_load` not required** | `Infini authors no document ops during a session` — **`@future` left** (Then still names `doc_load`) |
| Device commit → mirror apply p95 ≤300 ms (SRS-IN-08) | `applies well under p95 300 ms` (40 in-process `append_ink`; not USB-link) | no extra session-channels scenario |

## Paint source / viewer

- WorldLayer source is `TabletSession.paintMirror()` → `flattenDrawables` + transient `preview:*`. `rebuildWithRmInk` exists only as a withdrawn comment. No `rm-live-*` committed paths.
- Infini emits **0** document ops: `emitLocalStructureOp` returns `viewer_only`; `IpcRmTransport.sendDocOp` is a no-op. Viewport still published.
- SRS-IN-09 aliases `set_smart_transform` / `remove_node` share the same handlers as AC names `set_smart_group_transform` / `remove`.

## `@future`

| Scenario | Action |
|---|---|
| `append_ink from Epaper updates tree and WorldLayer` | **un-tagged** — unit tests pass |
| `Infini authors no document ops during a session` | **left `@future`** — second Then is epoch `doc_load` (IN-028) |

Do not fail IN-027 for leftover `@future`; unit tests cover the viewer AC without `doc_load`.

## Asks

1. Do **not** flip [STORY-IN-028](../stories/STORY-IN-028.md) `ready`. Handshake-gated `doc_load` stays next.
2. Lane C [STORY-EP-014](../stories/STORY-EP-014.md) remains in flight on `epaper/` — do not mix.
3. Next: `/sm` (or `/pm` gate) — QA signed off IN-027.

## Out of scope (unchanged)

- Handshake-gated `doc_load` (IN-028)
- Device tree / publish (EP-014)
- Residue EP-007…011 / IN-020…026
- Design package / `srs-ui.md`
