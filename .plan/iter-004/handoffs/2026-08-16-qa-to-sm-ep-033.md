---
from: qa
to: sm
date: 2026-08-16
iter: iter-004
cc: [dev, pm]
---

# Hand-off: QA → SM — EP-033 host PASS, device visual WAIT

## Context

[STORY-EP-033](../stories/STORY-EP-033.md) stays **`in-review`**. Hotfix has no `.feature`; ACs are device pen-down. Do not flip `done` until the human scores the four ACs on the binary now on the tablet.

Host analog: `ingest_origin_guard_test OK` (stale Press held; origin Move discarded even after a real Press; plausible Press has no extra gate).

## Device (USB `10.11.99.12` → `10.11.99.1`)

Deployed **2026-08-16 09:17** — `11309 ./epaper`, BuildID `a75e2e5f…`. `[sync] connected` to `:9877`; `[debug] connected` to `:9878`. Infini `npm run electron:dev` is live.

Live ingest this session: many `stroke_begin` → `append_ink` (AC4 host-side: strokes are committing). Logs cannot see a bottom-left `emitSegment`.

Infini does **not** inject pen samples (`viewport` / `drain_ack` / `doc_load` only). A diagonal on the panel is local ingest, not a desktop push.

## Asks

Human, on this binary:

1. Pen-down that used to paint a straight line from panel **bottom-left** to the tip — gone (SRS-EP-01).
2. Pen-down on a ToolChip tile (including after a stale first packet) — chip arms, **no** diagonal on the ink image.
3. No second stroke from widget-local `(0,0)` while the stylus is down.
4. Normal page stroke still starts under the tip (no extra lag beyond the ~8 ms flush).

Reply pass/fail. Pass → QA sets story `done` and handoff `/pm`. Fail → `DEF-*` + `blocked`.

## Constraints

Do not rewrite the coordinate map. Do not retune recognizers. Do not start EP-034 on `tabletcanvasitem.cpp` until this scores.

## Out of scope

IN-030 (signed off). Coordinate-map rewrite, Pen waveform, EP-032 chrome SM.
