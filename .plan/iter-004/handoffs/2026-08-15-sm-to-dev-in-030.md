---
from: sm
to: dev
date: 2026-08-15
iter: iter-004
---

# Handoff — SM → Dev — IN-030

## Context

Human **verified EP-031** on device (live re-warp; connector dirty-rect clip to the dragging
node’s box is fixed). Cursor **move one** → [STORY-IN-030](../stories/STORY-IN-030.md).

Infini is viewer + persistence: apply `create_connector`, derive the warp (ADR-0020). Do not
author connectors on the desktop. Shared fixtures with EP-030/031. BDD:
`.docs/modules/infini/features/vector-document/bdd/create-connector.feature`.

Campaign remaining: **0 divergent connector nodes** (REQ-07 / SRS-IN-09).

Optional second lane (WIP 2): [STORY-EP-033](../stories/STORY-EP-033.md) P0 origin/stale
pen-down — `infini/` vs `tabletappfilter.cpp` + ingest in `tabletcanvasitem.cpp`; no write
conflict. EP-034 stays queued.

## Asks

1. Mirror `create_connector` envelope (`from`/`to`/`warpStyle`/body/restShape) with 0 loss.
2. Same rest + ends + style → byte-comparable samples vs device.
3. `set_smart_transform` on a bound box → Infini emits **0** connector ops and re-derives.
4. Missing bound node → last-live pose, not invalid (D39).

## Constraints

Lock: vertical · verified. Forbidden: REQ-08, CHL-0011, CHL-0012, desktop connector authoring.
Do not copy EXP-0002 probe code. Do not retune enclose/membership.

## Out of scope

EP-032 chrome state machine. REQ-09 select / REQ-10 hand-touch (wait `/architect`).

## Next after you

**`/qa`** (host fixtures + Infini apply). Then `/pm` for campaign remaining vs EP-033.
