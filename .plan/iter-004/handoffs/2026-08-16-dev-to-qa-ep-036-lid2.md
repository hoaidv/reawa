---
from: dev
to: qa
date: 2026-08-16
iter: iter-004
---

# Hand-off: Dev → QA — EP-036 DEF-0001 (lid #2 ping timeout)

## Context

[STORY-EP-036](../stories/STORY-EP-036.md) still **`in-review`**. Bug 1 (connectors) is done. Bug 2 lid verdict waits on this.

Human: 1st lid reconnects (docs reset OK). **2nd lid:** Infini reset, epaper keeps doc, **not connected**, Mac `ping 10.11.99.1` timeout. 3rd lid / Quit Infini still dead.

Cause: restore only ran when the **tablet** lacked `10.11.99.1`. After Mac sleep the tablet iface can stay addressed while the Mac USB NIC is gone — we skipped UDC rebind.

Now gadget-down if **no carrier** or **ICMP to `RM_SYNC_HOST` fails**, even when local `10.11.99.1` is up. Infini-only down (peer ping works) still does **not** bounce (EP-034). Restore: configfs UDC → platform UDC → `g_ether` reload if still no carrier → usb0 → dropbear.

Log: `[sync] gadget-down — host USB dead or no 10.11.99.1 (EP-036)` then `gadget restore without unplug — re-enum UDC/g_ether`.

## Asks

Rebuild/deploy **epaper**. Two+ lid cycles with USB plugged:

1. After 2nd open, wait ~15s. Expect gadget-down log, then Mac ping `10.11.99.1` returns **without unplug**, then Infini RM connected.
2. Kill Infini while ping still works: must **not** bounce gadget (EP-034).

## Constraints

No ingest edits. Do not verdict Bug 2 until ping survives a second lid.
