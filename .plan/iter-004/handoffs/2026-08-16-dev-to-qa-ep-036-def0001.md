---
from: dev
to: qa
date: 2026-08-16
iter: iter-004
---

# Hand-off: Dev → QA — EP-036 DEF-0001 retry

## Context

[STORY-EP-036](../stories/STORY-EP-036.md) **`in-review`**. ifconfig-only restore failed. New sequence when gadget-down:

1. **UDC unbind/bind** (`/sys/class/udc/*/device/driver`)
2. **configfs UDC detach/reattach** (`/sys/kernel/config/usb_gadget/*/UDC`)
3. **usb0** `10.11.99.1` + autosuspend off
4. **dropbear** `try-restart` only if address is up

Does **not** bounce when `10.11.99.1` is already up. Cooldown 15s.

Host: `usbgadget_test OK` (order: udc-rebind → configfs-udc → usb0-addr → dropbear). Rebuild/deploy epaper.

## Asks

USB plugged, ping timeout: wait for `[sync] gadget restore without unplug — UDC rebind then usb0 (DEF-0001)` then ping `10.11.99.1` without unplug.

## Constraints

No ingest edits. EP-034 HOLD soak. IN-032 done.

## Out of scope

Reawa.
