---
from: dev
to: qa
date: 2026-08-16
iter: iter-004
---

# Hand-off: Dev → QA — EP-034 + EP-036 together

## Context

One device binary covers both classes.

| Class | Story | What shipped |
|---|---|---|
| Ping OK, Infini down | [STORY-EP-034](../stories/STORY-EP-034.md) `in-review` | TCP keepalive both ends; 2s StrokeSync retry; SSH keepalives on deploy |
| USB plugged, ping timeout / no `10.11.99.1` | [STORY-EP-036](../stories/STORY-EP-036.md) `in-review` | On-device classify; restore usb0 UP + `10.11.99.1` without unplug (≤10s); then same 2s TCP retry |

Logs: `[sync] gadget-up — TCP retry only (EP-034)` vs `[sync] gadget-down — no 10.11.99.1 (EP-036)` then `gadget restore without unplug` / `ok` / `failed`.

Host: `usbgadget_test OK`. Infini vitest 113 passed. No `tabletcanvasitem.cpp`.

## Asks

1. Rebuild/deploy epaper; restart Infini.
2. Ping-alive Infini-down: do **not** unplug; wait for `[sync] connected`.
3. When ping is dead with cable in: expect gadget-down log and restore; ping `10.11.99.1` returns without unplug, then session retry.

## Constraints

Do not bounce usb0 when `10.11.99.1` is already up. IN-032 demo mix is separate.

## Out of scope

Reawa. IN-032. EP-035.
