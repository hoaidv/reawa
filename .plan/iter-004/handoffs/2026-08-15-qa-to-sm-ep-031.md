---
from: qa
to: sm
date: 2026-08-15
iter: iter-004
cc: [dev, pm]
---

# Hand-off: QA → SM — EP-031 host PASS, device visual WAIT

## Context

[STORY-EP-031](../stories/STORY-EP-031.md) stays **`in-review`**. Do not flip `done` until the human scores the live-drag picture (stroke hole vs white slab). False-positive corpus (`@SRS-EP-20` Initiative 2) remains a **ship** gate, not this story.

BDD: `.docs/modules/epaper/features/connector-ink/bdd/connector-warp.feature`.

## Host

`./tests/run_device_document_test.sh` green, including `connector_warp_test` and `recog_warp_bench`.

| Scenario | Evidence |
|---|---|
| Morph I3 bitwise rest reconstruction | `test_i3_morph_identity` |
| Never-re-bake I1 0.000 u | `test_i1_never_rebake` |
| Delete keeps connector / undo glues (D39) | `test_d39_delete_keeps_connector` |
| Live re-warp on bound-node move (geometry) | `test_live_move_rewarp` |
| Recog + warp CPU vs SRS-EP-14/20 bars | bench p95 enclose 5 µs, UX1/UX2 ~50 µs, live refresh 2 µs — **not device p95** |

## Device (USB `10.11.99.12` → `10.11.99.1`)

Rebuilt ARM binary (stroke-punch origin hole) and deployed `2026-08-15 17:58`. Process `10342 ./epaper`. Debug + StrokeSync connected.

Live log after deploy (human on tablet):

| Stroke | Verdict |
|---|---|
| s-1, s-2 | enclose circle → SmartGroups |
| s-4 | **connector** `sg_enclose_s-1` → `sg_enclose_s-2` |
| s-5 | enclose ellipse |
| s-6 | **connector** `sg_enclose_s-1` → `sg_enclose_s-5` |

Repeated `[sync] vector rasterize` at **unchanged** ink/node counts after each connector (4/7 then 6/11) = bound-node **commit** path (`rasterizeVectors` on pen-up). Mid-drag must not full-panel; that is code-reviewed (`kSelectionGhostMinIntervalMs = 200`, origin hole + ToolCanvas only) — logs cannot see overlay dirt.

## UI spot-check (`conn.live_warp`)

[connector-chrome-live-warp.html](../design/connector-chrome/connector-chrome-live-warp.html): boxes + connector, **no** `ConnSelect`. Live paint draws group children + warped polyline + box handles; no `tgl.conn_style`. Matches Spec “style chip hidden”.

## Asks

Human, on the binary now on the tablet:

1. Drag a bound box. During drag: origin connector should erase as a **line**, not a white rectangle. Live curve on overlay. Mid-gesture e-ink speckle is BR-B15 (not a fail).
2. Pen-up: one settled picture, no ghost, not dirty.
3. Optional D39: delete one bound box — connector stays; undo glues.

Reply pass/fail. Pass → QA sets story `done` and handoff `/pm`. Fail → `DEF-*` + `blocked`.

## Constraints

Do not retune enclose/membership. Do not start REQ-08. Do not `--restore` until this check is done.

## Out of scope

EXP-0002 ≤2% FP ship gate. Infini mirror ([STORY-IN-030](../stories/STORY-IN-030.md)).
