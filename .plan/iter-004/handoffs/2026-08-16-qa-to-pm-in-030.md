---
from: qa
to: pm
date: 2026-08-16
iter: iter-004
cc: [sm, dev]
---

# Hand-off: QA → PM — IN-030 signed off

## Context

[STORY-IN-030](../stories/STORY-IN-030.md) **done**. Host BDD analog for `@SRS-IN-09`
(`.docs/modules/infini/features/vector-document/bdd/create-connector.feature`) — `npx vitest run tests/create-connector.test.ts`: **6 passed**.

| Scenario | Evidence |
|---|---|
| Envelope round-trip, 0 warped samples on the wire | `stores from/to/warpStyle/body/restShape…` |
| Shared rest → 0 divergent nodes (Morph I3 / never-re-bake I1) | `Morph at rest is bitwise…`, `never re-bakes rest` |
| `set_smart_transform` → 0 connector ops, geometry re-derived | `re-derives geometry and does not emit create_connector` |
| Missing bind → last live pose, not invalid (D39) | `stays drawn and is not marked invalid` |

Live session (Infini `electron:dev` + RM `10.11.99.1` on `:9877`) is up. This QA window saw `append_ink` / `create_smart_group` / `join_smart_group` / `set_smart_transform` — **no** `create_connector` on the ingest log yet. Host suite is the sign-off; a live connector draw is a nice extra, not a fail.

Residual (not a fail): sample byte-compare is Infini↔Infini, not a C++/TS hex golden on one process.

## Asks

PM gate-close for IN-030. Campaign W3 Infini mirror slice is complete on the host path.

## Constraints

Do not author connectors on the desktop. Do not start REQ-08.

## Out of scope

[STORY-EP-033](../stories/STORY-EP-033.md) device visual (separate handoff).
