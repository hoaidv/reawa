---
from: dev
to: qa
date: 2026-08-16
iter: iter-004
---

# Hand-off: Dev → QA — EP-034

## Context

[STORY-EP-034](../stories/STORY-EP-034.md) **`in-review`**. Ping-alive Infini-down class: TCP keepalive + existing 2s retry. Do not unplug.

## Asks

1. Infini `npx vitest run` — includes `tests/tcp-keepalive.test.ts`.
2. Device: after rebuild/deploy, confirm `[sync] connecting` → `[sync] connected` without USB replug while ping still works.
3. Deploy script: `ssh`/`scp` use ServerAliveInterval=15, ServerAliveCountMax=4, TCPKeepAlive=yes.
4. `--usb-stay` is ping-dead only — skip when ping works.

## Constraints

No `tabletcanvasitem.cpp` / recognizer edits. 2s StrokeSync retry unchanged.

## Out of scope

EP-035. Reawa USBWatcher.
