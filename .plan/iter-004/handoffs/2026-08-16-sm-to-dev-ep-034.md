---
from: sm
to: dev
date: 2026-08-16
iter: iter-004
cc: [qa]
---

# Hand-off: SM → Dev — EP-034 NOW (StrokeSync TCP)

## Context

Human started [STORY-EP-034](../stories/STORY-EP-034.md) (`in-progress`). WIP 2: EP-034 **∥** IN-030 live review. [STORY-EP-035](../stories/STORY-EP-035.md) queued.

Field 2026-08-16: `ping 10.11.99.1` **13/13, 0% loss**. Epaper and Infini **not connected**. That is the story’s **ping OK + Infini “RM disconnected”** bucket — **StrokeSync TCP**, not USB gadget death. Do not unplug.

IN-030 stays **`done`** (host BDD). Human still wants a live `create_connector` on the wire; that review is blocked until `:9877` comes back without a replug.

## Asks

1. TCP keepalive on device StrokeSync + debug-log `QTcpSocket`, and Infini listen `:9877` / `:9878`.
2. Confirm existing 2s retry reconnects after a genuine drop (SRS-EP-08).
3. SSH keepalives on `deploy-rm2.sh` still in scope (detect dead path); they are **not** today’s failure.
4. USB gadget stay-up notes remain for the ping-dead class — document, do not treat as the fix for this session.
5. No `tabletcanvasitem.cpp` / recognizer edits.

Then `/qa`. Human retries Infini connect + connector draw.

## Constraints

WIP: do not also start EP-035. Do not slice iter-005. Do not author connectors on the desktop.

## Out of scope

Reawa USBWatcher / `SSHSession.swift`. Gadget autosuspend as the primary fix for ping-alive.
