# Infini

Desktop infinity canvas (Electron + React). Product: `.docs/modules/infini/`.

See [RENDERING.md](./RENDERING.md) for transform, spatial index, and tile LOD.

```bash
npm install
npm test
npm run dev            # browser
npm run electron:dev   # Electron shell
```
