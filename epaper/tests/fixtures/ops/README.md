# Shared op fixtures (SRS-IN-09)

Identical JSON envelopes for **Infini (TS)** and **Epaper (Qt)** consumers.

Canonical copy also under:
`.docs/modules/infini/features/vector-document/fixtures/ops/`

| File | type | source |
|---|---|---|
| `append_ink.json` | `append_ink` | epaper |
| `create_primitive.json` | `create_primitive` | infini |

Envelope: `{ opId, type, payload, source }`.
